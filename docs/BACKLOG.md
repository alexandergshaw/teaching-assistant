# Backlog

GENERATED by `npm run backlog:render` from `docs/backlog.yml` - do not
hand-edit this file. `npm run backlog:check-generated` fails the moment it
drifts from a fresh render (Ruling BA-6). Edit `docs/backlog.yml` instead,
then re-render.

Four sections, one per `state` (backlog-automation.md section 3): who works
an item and how it leaves the queue depends only on which section it is in.
Closing an item DELETES its row from `docs/backlog.yml` - there is no
"done" state and no archive (Ruling BA-8); git history is the record.

## Actionable

Worked by an agent. Leaves this section when `verify` exits 0 and actually runs at least one assertion (Ruling BA-2).

| id | title | owns | verify | blocked_by | instrument | from | note |
|---|---|---|---|---|---|---|---|
| G6 | Both announcement-exemplar actions return an EMPTY SUCCESS for a blank courseId, before querying. src/app/actions/walkthrough-announcement.ts:122 returns {exemplar: null} and :141 returns {exemplars: []}, each above its createServiceClient() call - so "I did not query" reaches the client in the same shape as "I queried and this course has none". That is the same false-claim family G1 exists to close, one layer deeper: the client cannot tell them apart no matter what state machine it runs, because the distinction is already gone at the boundary. | src/app/components/walkthrough-announcement/WalkthroughAnnouncementPanel.tsx, src/app/components/walkthrough-announcement/walkthrough-announcement.structure.test.ts | npx vitest run src/app/components/walkthrough-announcement/ | - | REACHABILITY SETTLED 2026-09-14 by tracing courseId to its source, which is what this row was blocked on - and the answer RELOCATES the fix. NOT reachable through the UI: the course control is a MUI Select over the fetched courses list (AnnouncementCourseFieldset.tsx:124-127, onChange passes e.target.value from a MenuItem whose value is c.id), so it can only ever emit a real course id. REACHABLE only through localStorage: courseId is seeded by an unvalidated, untrimmed initializer - window.localStorage.getItem(STORAGE_KEY_COURSE) ?? "" at WalkthroughAnnouncementPanel.tsx:165-167, key "ta-rec-wta-course" (:97) - and written back at :171. A whitespace-only value there is TRUTHY, passes the panel guards at :294 and :350, reaches both actions, and gets an empty SUCCESS back before any query. So this is a LATENT boundary defect, not a live one: producing it requires hand-editing one's own localStorage, and the only victim is that same user. Note the realistic neighbour is NOT this defect - a STALE but non-blank course id does reach the query, returns zero rows honestly, and is correctly an empty result. Original finding: the G1 data pass's adversarial check, confirmed by direct read of both actions. Original instrument note: Found by the G1 data pass's adversarial check and confirmed by direct read of both actions, 2026-09-14. REACHABILITY IS UNPROVEN and must be settled before this is worked: the panel guards both call paths with if (!courseId) return (:276-282 and :318), so an EMPTY string never reaches the action. A WHITESPACE-ONLY courseId is truthy, passes that guard, and gets a clean empty list back - but whether this surface can produce one was not traced. Settle that first; if it cannot, this is a latent boundary defect rather than a live one, and should be scoped accordingly. | deferred out of G1, 2026-09-14 | SCOPED 2026-09-14, and the fix MOVED on the evidence. The deferral reasoning below said the cheap fix is a distinct non-success arm on both actions - that is now the EXPENSIVE fix and it is not the one to build: it changes the RETURN UNION of two server actions, touching every caller, to close a state only a hand-edited localStorage can produce. The CHEAP and correct fix is at the SEED instead: trim the localStorage value in the initializer at :165-167, so a whitespace-only stored value becomes "" and is caught by the panel guards that already exist at :294 and :350. One expression, no union change, no new caller obligations, and it makes the unreachable state unrepresentable rather than handled. The boundary arms in the actions stay on record as a hardening option if a SECOND caller ever appears that does not guard - today the panel is the only caller (grep -rn "listAnnouncementExemplarsAction" over src/app/components returns the panel alone). Original deferral note follows. DEFERRED OUT OF G1 DELIBERATELY, not dropped. Three reasons, recorded so the deferral can be judged: reachability is unproven (above); the fix changes the RETURN UNION of two server actions, so it touches every caller rather than staying inside one surface; and G1's wave was already the widest part of that item. The cheap fix is a distinct non-success arm for the not-queried case, mapped by the client to a state that is not "loaded" - so neither "No saved exemplars for this course yet." nor "(no longer available)" can be said off the back of a query that never ran. |

## Owner decision

Never worked by an agent. Needs a human decision; the owner answers.

_None._

## Verification (owner)

Never worked by an agent. Needs a live system or a real credential; the owner reports.

| id | title | owns | verify | blocked_by | instrument | from | note |
|---|---|---|---|---|---|---|---|
| V1 | Look at the announcement preview. Its appearance is a reading claim - the styling is ported from course-intel.module.css and reviewed by eye, which is how the unstyled version passed review the first time. Check that bullets indent, paragraphs separate, and links look like links. | - | - | - | Open the walkthrough announcement panel | b3701cd | - |
| V2 | Paste a copied draft into Canvas's editor. Whether a real browser accepts the multi-type clipboard write, and whether the rich flavour arrives as bold rather than asterisks, cannot be tested here: of the 9 it( blocks in src/app/components/ui/clipboard.test.ts (grep -c "  it(" ), the ones exercising ClipboardItem install a stub class, and one deliberately stubs it as undefined to pin the fallback. | - | - | - | Copy a draft, paste into Canvas | b3701cd | - |
| V3 | Read the orphan sweep's first production tick's raw body. The delete accounting ships OBSERVATIONAL ONLY because what remove() returns for a named delete is unmeasured; matchedByLeafName / matchedByFullPath exist to settle it in one tick. Unblocks N3 (formerly 3.2). | - | - | - | The first run's JSON body in the Actions log | c25bdec | - |
| V4 | Confirm the sweep's prefix form matches. A ruling pinned ${userId}/${segment} with no trailing slash; only a live listing proves Supabase agrees. Signature of a wrong guess: a positive scannedUserPrefixes with every other count at zero. | - | - | - | Same tick as V3 (formerly 1.3) | c25bdec | - |
| V5 | Confirm the scheduled workflow fires. | - | - | - | The Actions web UI after merge | c25bdec | - |
| V6 | Check whether a killed tick leaves its start-of-tick line. src/app/api/cron/sweep-orphan-uploads/route.ts:92 logs it; the sibling run-schedules/ route chose a durable DB write instead, precisely because a tick killed at the platform cap never runs finally. | - | - | - | Vercel function logs after the first timeout | c25bdec | - |

## Unscoped

Lacks `owns`/`verify` (or both). Nobody works it until someone gives it both - fabricating either is worse than leaving it here (Ruling BA-2).

| id | title | owns | verify | blocked_by | instrument | from | note |
|---|---|---|---|---|---|---|---|
| G2 | Snapshot grading's restored list is seeded from a localStorage-reading useState initializer on an SSR'd surface. The identical initializer already ships elsewhere and a structural mismatch recovers by re-render, so this is inherited rather than new. | - | - | - | - | 82f5bde | - |
| G3 | The sweep's per-call latency assumption (~250ms, src/lib/orphan-upload-sweep.ts:46,55) underpins both MAX_USER_PREFIXES_PER_TICK and PHASE1_ROOT_LISTING_BUDGET_MS. A wrong assumption makes the sweep UNDER-clean, which is the safe direction. | - | - | - | - | c25bdec | First tick's timing (pre-migration Note column) |
| G4 | An LLM call with no timeout runs under the platform default, not 60s. callLlm takes no AbortSignal, so nothing bounds a first attempt; learning-resource-links.ts:55-66 documents a 90-100s case and says the mitigation is RETRY_BUDGET_MS = 32_000 (:121), which gates RETRY decisions and does not bound the first attempt. Compounding it: src/app/page.tsx exports NO maxDuration (grep -n maxDuration src/app/page.tsx returns nothing), and this repo already records in its own comments (src/app/api/automations/run-now/route.ts:35-37, course-intel/ask/route.ts:48-59) that Next honours maxDuration for Server Actions only at the PAGE level. So these actions run under the platform default rather than the 60s Hobby figure. When the platform kills the invocation the action's own catch never runs, so the client gets a raw transport failure instead of the {error} string the action would have returned. Reachable today via discussion-replies.ts:786 and learning-resources-generator.ts:229. Recorded because two separate G3 design passes found it and filed it nowhere. | - | - | - | RE-MEASURED 2026-09-14 and the row's framing does not survive it. THREE findings. (1) BLAST RADIUS is understated ~60x: the row names two reachable paths, but grep -rn "callLlm(" src/ excluding tests returns 124 call sites. callLlm (src/lib/llm.ts:375) takes (req, provider) and no AbortSignal, and llm.ts contains ZERO AbortSignal or timeout references - the single "signal" hit at :718 is prose about something else. So a call-level bound is a 124-site seam change, or it is optional and therefore unenforced. (2) THE PAGE-LEVEL OPTION IS NOT AVAILABLE. src/app/page.tsx is "use client" (line 1), and a client component cannot export route segment config at all - maxDuration is a server-only export. So "page-level maxDuration" is not a cheaper alternative to a call-level bound; it is not on the table without converting the page to a server component. (3) THE REPO ALREADY HAS A THIRD ANSWER and documents it: move the feature off a Server Action to a Route Handler, which CAN declare its own ceiling. Twelve route handlers under src/app/api/ already declare maxDuration, and course-intel/ask/route.ts:48-59 plus automations/run-now/route.ts:35-38 explain they did it FOR EXACTLY THIS REASON. That file also records the constraint any fix inherits: prod is Vercel Hobby, whose hard cap is 60s regardless of what a route requests, and a value above 60 makes the deployment FAIL TO BUILD. | c25bdec-era, pre-existing | STILL UNSCOPED, and the owner decision it needs has CHANGED shape - the old either/or was a false dichotomy (see instrument). The real fork, and it is a genuine product/cost call rather than something an agent should default: MIGRATE the two named reachable features (discussion-replies.ts, learning-resources-generator.ts) off Server Actions onto Route Handlers that declare their own sub-60s ceiling - which is real work and changes how the client calls them, but follows a pattern three routes here already established and documented - OR accept the unknown platform ceiling and the raw transport failure it produces. NOTE the defect itself is NOT the slow call; it is that when the platform kills the invocation the action's catch never runs, so the client gets a transport failure instead of the {error} string the action would have returned. A bound must therefore sit BELOW the invocation ceiling for the action to return anything at all - which is why knowing the ceiling and bounding the call are two halves of one fix, not alternatives. NOT to be worked as one chunk: at 124 call sites, any callLlm signature change is a repo-wide seam and needs its own chunking. |
| N3 | Flip failedCount to gating in .github/workflows/sweep-orphan-uploads.yml. A one-line edit an agent makes here; blocked on V3's (formerly 1.3's) INPUT, not on capability. Until the semantics are known, a wrong guess in either direction poisons the only outbound alert this deployment has. | - | - | V3 | - | - | Blocked by V3 (formerly 1.3) (pre-migration queue note) |
| R2 | Reclassify every requireOwner() call site into requireUser() or requireAppOwner(). requireOwner is `return requireUser()` (src/lib/supabase/auth.ts:451-452) - any active account passes - across 446 invocations (grep -roE "await requireOwner\(\)" src/ \| wc -l). Its @deprecated block calls this "DELIBERATELY LESS RESTRICTIVE ... a tracked, temporary state". CORRECTION, measured 2026-09-14: an earlier version of this entry said requireAppOwner() has ZERO call sites. That is FALSE and I had copied it from docs/multi-user-login-acceptance-criteria.md:889 without opening the tree - the same defect this file's own header rule exists to prevent. requireAppOwner appears in 15 files (grep -rcE "requireAppOwner\(\)" src/ \| grep -v ":0" \| wc -l), with 9 direct await requireAppOwner() invocations by my count; the scoping pass counted more by a broader pattern, so re-measure before relying on either number. Group C wired it into the account-admin actions, gated by action-guard-coverage.test.ts's OWNER_ONLY map. The scoping pass also found the bigger constraint: the architecture doc's RESOLVED 3 / SUPERSEDES section already TRIED per-call-site classification for the Canvas/GitHub cohort and REJECTED it, choosing resource-level assertions inside canvas-core.ts's resolvers and github.repos.ts's ghFetch instead - and I confirmed both files have zero guard calls today, so that replacement design is on record but UNBUILT. A literal call-site reclassification there would contradict a decision this repo already made. Scoping in scratchpad b44-scope.md; first wave proposed is the media-file cohort (42 call sites, 4 files) where call-site and resource-level assertion coincide. | - | - | - | - | docs/multi-user-login-acceptance-criteria.md A6 | Its own wave (pre-migration Trigger column) |
