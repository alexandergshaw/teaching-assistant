# Backlog

GENERATED by `npm run backlog:render` from `docs/backlog.yml` - do not
hand-edit this file. `npm run backlog:check-generated` fails the moment it
drifts from a fresh render (Ruling BA-6). Edit `docs/backlog.yml` instead,
then re-render.

Closing an item DELETES its row from `docs/backlog.yml` - there is no
"done" state and no archive (Ruling BA-8); git history is the record.

Three sections, one per `kind`: Bugs, Features, Chores, in that order,
always all three even when empty. `area` (first column) clusters related
rows by adjacency, in the order fixed by `src/tools/backlog/areas.ts` -
never alphabetically. `state` (a column, not a section) is one of:
- `actionable`: Worked by an agent. Leaves this section when `verify` exits 0 and actually runs at least one assertion (Ruling BA-2).
- `owner`: Never worked by an agent. Needs a human decision; the owner answers.
- `verification`: Never worked by an agent. Needs a live system or a real credential; the owner reports.
- `unscoped`: Lacks `owns`/`verify` (or both). Nobody works it until someone gives it both - fabricating either is worse than leaving it here (Ruling BA-2).

Owner ruling (2026-09-15): a row that is both a live defect and an open
owner question reads under Bugs - not under a separate section and never
left blank.

## Bugs

| area | id | state | title | owns | verify | blocked_by | instrument | from | note |
|---|---|---|---|---|---|---|---|---|---|
| Grading runs: bounds, failures, not-attempted rows, class-trends signal | A12 | unscoped | Grading more than the configured bound of GitHub repos at once no longer silently drops the rest - engine.ts appends a re-runnable not-attempted row per dropped repo (7c409a6) - but nothing in the review table shows that row's reason to the instructor, the same visible-state gap A13-2 owes. | - | - | - | DEFAULT_MAX_SUBMISSIONS = 40 confirmed at src/lib/gemini.ts:32 (row cited :25, stale); engine.ts:311-323 (submission-count-bound tail) and :297-310 (deadline tail) confirmed appending an UngradedResult row with a re-run message for every entry the run does not attempt, referenced 2026-09-15 against the current tree. UI sweep (Select-String for not-attempted/isUngraded/.ungraded across src/app/components, 2026-09-15) found zero rendering of these rows' reason anywhere, including GithubGradingPanel.tsx and GradingResults.tsx. | N13a scoping check, 2026-09-15, re-confirmed hop by hop by the orchestrator | SILENTLY DROPS is no longer accurate - 7c409a6 made the drop a first-class, re-runnable row at the data layer. What remains is identical to A13-2: no UI surface renders the not-attempted reason or offers a re-run action, so an instructor scanning the results table still cannot tell a dropped repo from a graded one without reading source. blocked_by N13a should be REMOVED - N13a's cap/deadline decision (the thing A12 was waiting on) already landed; N13a's remaining scope (the class-trends-draft FLOOR) does not gate this row. Converge this with A13-2 rather than scoping a second visible-state mechanism; do not default a fresh design for the same gap. |
| Grading runs: bounds, failures, not-attempted rows, class-trends signal | A13 | unscoped | A13-2 remains: a failed or bound-stopped submission has no visible un-postable state or re-run control in the review table. A13-1 (checkRowPostability, ac8002b) already stops the internal error text reaching students - this row is what is left, and LIVE ON MAIN TODAY no longer applies. | - | - | - | checkRowPostability wiring confirmed at grading.ts:529, GradingResults.tsx:288/390, repoGradesPosting.ts:350, RepoGradeCellControl.tsx:289, steps.grading-draft-flow.ts:615, steps.grading-singles.ts:597/607 (git show ac8002b --stat plus a per-file Select-String, 2026-09-15); GradingResults.tsx:256-260 confirms the ungraded-exclusion comment verbatim; a repo-wide grep for not-attempted/isUngraded/Re-run/gradeOneSubmissionAction usage in src/app/components turns up no UI rendering and no re-run control anywhere. | N13a round-2 check, 2026-09-15, re-read hop by hop by the orchestrator | A13-1 SHIPPED (commit ac8002b, 2026-09-15) and is confirmed wired at every payload builder the original row named, so the title's LIVE ON MAIN TODAY claim about internal error text reaching students is now FALSE and must not be repeated. A13-2 is the sole remaining scope: the review table (GradingResults.tsx and its row renderer, plus the repo-grades equivalents) must mark a postable:false row as a DISTINCT visible state rather than rendering it like an ordinary graded row, and must offer a re-run control wired to gradeOneSubmissionAction (grading.ts:597, currently called only from steps.grading-singles.ts:238, a workflow step, never from UI). Re-runnability needs identity - GradeResult carries no id field (confirmed: no id or id-optional property in src/lib/grade/types.ts) - so whether a failed/ungraded row can be re-dispatched, and whether that identity survives a reload, is still to be established, not assumed. This converges with A12's identical residual (a visible not-attempted disclosure plus re-run), and the two should not invent separate mechanisms. |
| Grading runs: bounds, failures, not-attempted rows, class-trends signal | A14 | unscoped | A student's zip loses the student. On a Canvas bulk download where students submitted .zip files, every student's main.py collapses into ONE row named "main" - contents concatenated, one merged submission graded, the other students silently absent from the results table. LIVE ON MAIN and grade-affecting. extraction.ts:60-68 recurses into a nested zip with parentPath = the outer zip name (which carries the student identity), then utils.ts:77 takes getBaseFileName(...) = "main.py" and DISCARDS the parent segment; parts.length is then 1, the >= 4 Canvas branch at :94-106 is skipped, and the fallback at :108-117 yields studentKey "main". groupSubmissionsByStudent:151-163 groups on that key and :169-174 concatenates. | - | - | - | Traced end to end by the N15a seam check, 2026-09-15, which read every cited line. REACHABILITY MEASURED, and it is not an edge case: provider "embedded" on the zip path is DETERMINISTIC - grading.ts:833 calls extractStudentEntries and extraction.ts:125 passes undefined for the inferred lookup, and that path has no LLM rescue by design ("must never depend on a model"). provider "gemini" hits the same fallback whenever inference degrades - engine.ts:411 inferFileNameConvention returns an empty lookup on !result.ok, on a JSON parse failure, and per item on anything failing parseInferredFileNameLookup filters (rubric.ts:171-178). UNTESTED: src/lib/grade/utils.test.ts is 68 lines (@(Get-Content).Count) and contains ZERO tests for parseSubmissionFileName or groupSubmissionsByStudent. | N15a seam check, 2026-09-15 | FILED SEPARATELY AND DELIBERATELY, out of the N15a design that found it: this is live on main, independent of any queued feature, and it changes grades rather than presentation - so it must not be entangled with a feature whose shape is still moving. THE FIX IS NOT OBVIOUS AND SHOULD BE DESIGNED, NOT GUESSED: the parent path segment is the identity carrier on the nested-zip path, but the Canvas 4-part convention branch and the inferred-lookup branch both key off the leaf, so a naive "prefer parentPath" changes behaviour on paths that work today. FIRST STEP, cheap and owed before any fix: write the missing tests against utils.ts for both functions with a nested-zip fixture, and prove they FAIL on the current code - this repo has shipped a guard test that could not fail. THE OWNER-VISIBLE SYMPTOM, for triage: students missing from the results table entirely, and a row whose student name is a bare filename stem like "main" or "submission". TWO CORRECTIONS TO THIS ROW, both measured during the fix and both to claims made here rather than by the implementer. (1) A COST MODEL UNDERSTATED: a coincidental 4-part leaf inside correctly-named student zips creates a phantom row that STEALS THAT FILE FROM EVERY STUDENT, not "one file" as first priced - measured, a path ending src/my_test_file_here.py yields a merged row named "my", and that shape is ordinary in real CS submissions. Unchanged from HEAD so not a regression of the fix, and deliberately left unfixed. (2) STILL OPEN AFTER THE FIX: two different students whose zips share an identical non-Canvas name still merge, and separating them by the field after the student name was ATTEMPTED AND WITHDRAWN - the evidence cited for it was this app's own synthetic packer (canvasWorkToZipBase64), not a real Canvas archive, and utils.ts:70 says the real convention puts a DATE in that position, which would have split ONE student into two UI-identical rows sharing one edit entry and therefore one grade. It needs one real Canvas submissions zip's filenames from the owner to settle; nothing in this checkout can supply that. Also measured and unfixed: files nested past MAX_NESTED_ZIP_DEPTH = 3 vanish with no failedSupportedFiles entry. |
| assessment-shared: row restore and feedback composition | A10 | actionable | Copying the overall comment on the recording grading page yields every part TWICE, because the copy joins the individual feedback fields AND the composed field that already contains them. Owner, 2026-09-15, correcting an earlier request that had been filed as a removal. | src/app/components/grading-recording/grading-row.ts, src/app/components/assessment-shared/AssessmentFeedbackFields.tsx, src/app/components/grading-recording/GradingTableRow.tsx, src/app/components/snapshot-grading/SnapshotResultCard.tsx, src/app/components/grading-recording/copy-feedback.test.ts | npx vitest run src/app/components/grading-recording/copy-feedback.test.ts | - | Direct read of src/app/components/assessment-shared/assessment-row.ts:62-109 and a grep for overallComment across src, 2026-09-15 | owner request, 2026-09-15 | FILED WITHOUT owns AND verify DELIBERATELY, because the grep says this is not a one-file deletion and a file list written from the request would be wrong - which is L10's exact class, five instances today. overallComment is referenced across at least four modules outside the component: grading-run-mapping.ts:47,54 composes it, grading-submission-grade.ts:7 documents it as part of the action's return shape, grading.ts:518 posts it as the Canvas comment, and assessment-row.ts:109 joins it with strengths and improvements into the text that is actually posted. THE QUESTION THAT DECIDES THE SIZE, and the owner should answer it rather than have it defaulted: remove the TEXTBOX from the page but keep the field (the model may still produce one and it would still be posted), or remove the FIELD end to end (the type, the composer, the posted-comment join, and whatever tests pin the three-field shape)? The first is small and leaves a value the instructor can no longer see or edit being sent to students - which is worse than it sounds, and is the same hazard pattern as a stale disclosure. The second is a type change across the assessment-row contract, which assessment-shared is named for sharing - grep says snapshot grading and repo grades both build on that module, so the blast radius must be measured before a file list means anything. OWNER DECISION 2026-09-15, asked and answered: REMOVE THE FIELD END TO END - "remove the field end to end (type change across assessment-row". So this is the larger of the two readings, deliberately: the type, the composer (grading-run-mapping.ts:47,54), the action's documented return shape (grading-submission-grade.ts:7), the posted-comment join (assessment-row.ts:109), the Canvas post (grading.ts:518) and every test that pins the three-field shape. The rejected alternative was leaving a value the instructor can no longer see or edit being sent to students. BLAST RADIUS MUST STILL BE MEASURED before a file list means anything - assessment-shared is named for sharing, and snapshot grading and repo grades both build on it. BLOCKED IN PRACTICE, not by dependency: A9 is being worked in grading-recording/ and assessment-shared/ right now, and this must not run concurrently against the same files. CORRECTED 2026-09-15, BY THE OWNER, AND THE CORRECTION CHANGES WHAT THIS ITEM IS. Verbatim: "don't remove the overall comment per se, it's just that when i copy the overall comment for one of the latest pages, i get the same comment twice (i assume because it's copying the individual components and then the combined field)". So SUPERSEDE both readings above and the REMOVE THE FIELD END TO END decision recorded before this: the field stays, and the defect is a DUPLICATED COPY. The owner's guess at the mechanism is exactly right and has been confirmed by direct read. composeOverallComment (src/lib/grade/types.ts:20-29) joins strengths, improvements and resubmitNotice with a space - overallComment is DERIVED, never authored, and grading-feedback-prompt.ts:153 builds the recording surface's rows that way. joinAssessmentFeedback (assessment-row.ts:108) then joins [strengths, improvements, overallComment] with blank lines, and AssessmentFeedbackFields.tsx:108 is the Copy feedback control that calls it. So a copy is strengths + improvements + (strengths + improvements + resubmitNotice): every part twice. THE ORIGIN IS A STALE CONTRACT, which is the reusable lesson: joinAssessmentFeedback's own doc comment cites CC14 as requiring exactly those three fields, and that rule was written while overallComment was still INDEPENDENTLY AUTHORED. A1 item 2 (documented at grade/types.ts:10-13) later made it derived, and nobody revisited the join that had been correct under the old contract. Grep for other readers of overallComment that also read strengths or improvements in the same breath - there may be more than this one. THE FIX IS NOT SIMPLY DROPPING overallComment FROM THE JOIN, and this is the trap: AssessmentFeedback (assessment-row.ts:64-69) carries totalScore/strengths/improvements/overallComment and has NO resubmitNotice field, so on this surface the resubmit notice exists ONLY inside the composed overallComment. Drop overallComment from the join and the copy silently loses the resubmit notice - which grade/types.ts:137-140 calls a promise about instructor policy that every producer must say identically or not at all. Losing it from a copied comment is a worse defect than the duplication being fixed. PROPOSED, and to be checked rather than adopted: give AssessmentFeedback its own resubmitNotice field and join [strengths, improvements, resubmitNotice], which matches GradeResult's shape and continues the same each-part-is-its-own-field direction A1 item 2 already took. The alternative of copying overallComment ALONE is content-complete but space-joined, so the copy regresses to one run-on paragraph instead of blank-line-separated sections. Still an additive type change across assessment-shared, so blast radius must be measured. STILL SEQUENCED BEHIND A9, which is being worked in the same two directories.CROSS-SURFACE DEFECT IN THIS ROW'S OWN CANDIDATE FIX, found 2026-09-15 by the A11 scoping check and recorded here because it is A10's to carry, not A11's. THE CANDIDATE on file is to give AssessmentFeedback its own resubmitNotice field and join [strengths, improvements, resubmitNotice]. EXECUTED against the real function on the OTHER surface that shares it: assessment-row.ts:109 today returns [row.strengths, row.improvements, row.overallComment] filtered, and joinAssessmentFeedback is reached from BOTH GradingTableRow.tsx:263 and SnapshotResultCard.tsx:188 via AssessmentFeedbackFields.tsx:35,108. On the snapshot surface strengths is the hardcoded empty literal (useSnapshotGrade.ts:195 - note :195, not the :194 the A11 row cites) and no resubmitNotice exists anywhere in either directory (grep -rn "resubmitNotice\|RESUBMIT_NOTICE" exits 1, against a canary of overallComment over assessment-row.ts which hits 3 times). So the candidate join yields IMPROVEMENTS ONLY on snapshot grading, and the model's authored overallComment - which on that surface is the whole of the feedback prose, since the shared prompt at prompts.ts:69-70,90 instructs the model to fold praise and deduction reasons into that one block - DISAPPEARS FROM EVERY SNAPSHOT COPY FEEDBACK. Silently, and with every gate green: nothing renders a component here, and assessment-row.test.ts:49,54,59 exercise joinAssessmentFeedback against hand-written fixtures that never compose a snapshot row. WHAT THIS ADDS TO THIS ROW, and it is a scope fact rather than a caution: A10 cannot be scoped against the recording surface alone. Any change to joinAssessmentFeedback or to the AssessmentFeedback shape is a TWO-SURFACE change by construction, its owns list must carry the snapshot callers, and its oracle must compose a row from each surface - a fixture-only test cannot see this class of defect at all. A11 IS NOW BLOCKED BY THIS ROW for the same reason, and its own rulings section records why the sequencing runs this way. SCOPED AND CHECKED 2026-09-20; check returned DEFECTIVE (5 blockers) and ordered a normal revision round. CONFIRMED SOUND, do not re-derive: the 8-hop copy trace; the central claim (assessment-row.ts:109 joins [strengths, improvements, overallComment] while overallComment is already strengths + " " + improvements + " " + resubmitNotice, so the clipboard gets every part twice); that resubmitNotice is computed at grading-feedback-prompt.ts:152 and DISCARDED at :155, surviving only inside overallComment; and that joinFeedback has ZERO production callers (15 grep hits: 1 definition, 3 doc comments, 11 in its own test). THE ROW''S OWN TEXT IS OVERTURNED AND THE CHECK AGREES: A10 is RECORDING-ONLY, not two-surface. Snapshot is free of the defect on three independent grounds - useSnapshotGrade.ts:264 hardcodes strengths to empty, snapshot-parse.ts:222-223 reads overallComment and improvements as independent model fields with no composition, and prompts.ts:90,93,94,99 forbid the model repeating material across them while :106 forbids mentioning resubmission at all. THE BLOCKER THAT MATTERS MOST (silent green): a per-surface resubmitNotice CANNOT CROSS applyAssessmentResult. grading-rows.ts:155-157 delegates to assessment-row.ts:146-163, which spreads source and overwrites EXACTLY FOUR fields - totalScore, strengths, improvements, overallComment - and assessment-row.ts is NOT in the proposed owns. So a literal implementation gives: fresh rows lose the notice on every copy (the defect the fix exists to prevent), and re-graded rows keep a stale notice, so a full-credit re-grade can still tell a student to resubmit. Both ship with every gate green, and NO file in the proposed verify observes that seam. OTHER BLOCKERS: the first sabotage mutates joinFeedback, which the artifact itself proved dead and whose only test is not in the verify, so it reds nothing; the pass condition "Test Files 6 passed (6)" is prose - closure-runner.ts matches the Tests line not Test Files, and runVerify has zero production callers; grading-feedback-prompt.test.ts:108-118 is the ALREADY-LANDED enforcer of the full-credit-no-notice behaviour and is in neither owns nor the verify; and the legacy back-fill keys on the LIVE RESUBMIT_NOTICE constant, so rewording that user-visible string silently strands every pre-fix row - it needs a frozen dated tombstone literal, and the predicate must fire on ABSENT, never on empty, or it resurrects a notice an instructor deliberately cleared. ROUTED TO THE OWNER, not to another round: after this fix the recording surface''s Overall comment box becomes WRITE-ONLY - it is editable at AssessmentFeedbackFields.tsx:154-163, nothing on that surface posts to an LMS (GradingRecordingPanel.tsx:679 says so outright), the log excludes it and offline-payload hardcodes it blank. Drop the box here, fold it into copyText, or knowingly keep a dead field. NOTE this is the same class as A11 and lands on the SAME shared component, so answer them together. Also measured: that box already shows a STALE composition today, because editAssessmentField (assessment-row.ts:120) sets one field without recomposing, unlike both sibling surfaces (gradingResultsHelpers.ts:333, repoGradesCellEdits.ts:196). See the A11 row for the shared ruling covering this row''s write-only Overall comment box: it STAYS and is folded into copyText. Do not scope a field-omission mechanism for it. ROUND 2 CHECK 2026-09-20: DEFECTIVE, 5 blockers. TWO ORCHESTRATOR RULINGS, taken not asked. RULING 1 - TAKE THE RE-DERIVATION; DO NOT ADD A FIELD. The check found the smallest correct design and the revision never considered it: THE NOTICE IS ALREADY RECOVERABLE FROM THE ROW AS IT STANDS. On this surface overallComment IS composeOverallComment(strengths, improvements, resubmitNotice) - a blank-filtered space join (grade/types.ts:154-162) - and RESUBMIT_NOTICE is a FROZEN constant the prompt forbids the model from producing (prompts.ts:106). So the copy derives it: copyText = [strengths, improvements, and the notice when overallComment ends with RESUBMIT_NOTICE], blank-filtered, joined by a blank line. NO new field, NO wire key, NO EXPECTED_WIRE_KEYS bump, NO six-hop carriage through GradingRecordingFeedback / GradingRecordingResult / GradingResultInput / applyAssessmentResult, and NO legacy migration - because the derivation IS the read path rather than a one-time back-fill. This dissolves three of the five blockers instead of carrying them as obligations: the cross-the-seam repeat, the oracle whose power depended on an unpinned value, and the migration predicate specified without its value. The clinching argument: the back-fill blocker forces this derivation to be written ANYWAY for legacy rows, so writing it once as the permanent path is strictly less work. owns collapses to roughly grading-row.ts, grading-row.test.ts, AssessmentFeedbackFields.tsx and its two callers. RULING 2 - "FOLDED INTO copyText" INCLUDES A DIVERGED overallComment. The check is right that the previous design satisfied my earlier ruling only where it was vacuous: editAssessmentField (assessment-row.ts:120-129) writes overallComment WITHOUT recomposing and sets userEdited, and AssessmentFeedbackFields.tsx:154-163 is an editable box wired to exactly that - so an instructor''s hand-typed overall comment persists forever, is protected from re-grade by the userEdited guard, and under a three-field join would never appear in any copy. That is the dead field I rejected. THE RULE: when overallComment is NOT the derived composition of the row''s own parts, it has diverged and the instructor typed it, so it goes into the copy as its own section. When it IS the derived composition, it is redundant and is omitted - which is the whole point of this row. ALSO CARRY: (a) a rewrite rule mandating that every joinFeedback case go through composeGradingRowResult would DELETE coverage - four of the seven landed cases (grading-row.test.ts:70, :83, :88, :100) describe shapes the composer cannot emit, and :93-99 says in its own comment that the all-empty case exists to exercise the captured-but-ungraded shape. Composition cases go through the composer; edge-shape cases keep literals, because a freshly captured row IS an emitted shape. (b) No test in src references AssessmentFeedbackFields or SnapshotResultCard, and the only test reading GradingTableRow.tsx is markLate.wiring.test.ts:31 on an unrelated subject - so the caller-substitution sabotage reds ONLY if the new seam test readFileSync-es both callers and pins the argument. Say that; do not name an existing file. (c) The required prop prevents OMISSION, not SUBSTITUTION - there is no type-level defence against passing the duplicating expression. (d) grading-feedback-prompt.test.ts:108-119 asserts only on overallComment, so it cannot fail for the new behaviour; being in owns is not enough. (e) A doc comment on assessment-shared code justifying this by naming the snapshot surface turns assessment-shared.structure.test.ts red - its FORBIDDEN_WORDS scan is raw source.includes at :50, so comments count. (f) assessment-row.test.ts:95-142 holds the only four direct tests of applyAssessmentResult and was named nowhere. (g) The A11 ruling''s claim that assessment-row-store.test.ts:173-185 is owed is WRONG - :165-170 documents it running over a FIXTURE codec, deliberately not the real GradingRow codec. RE-SCOPED 2026-09-20 on the derivation; check in flight. TWO CORRECTIONS TO MY OWN RULINGS, both found by the scoping seat. (1) RULING 1''S endsWith PREDICATE DOES NOT SURVIVE RULING 2, and must not be implemented as worded. endsWith(RESUBMIT_NOTICE) plus "a diverged overallComment is copied whole" produce a CONTRADICTORY CELL: an instructor who pastes the notice into their own hand-written comment satisfies both, so the copy emits the notice as its own section AND again inside the appended comment - a fix for a duplication defect that duplicates. THE PREDICATE IS BYTE EQUALITY against the two candidate compositions: overallComment has DIVERGED iff it equals neither composeOverallComment(strengths, improvements, "") nor composeOverallComment(strengths, improvements, RESUBMIT_NOTICE), computed by CALLING the production function on the row''s OWN current parts. That makes classification and notice-recovery one 3-way total function that cannot disagree with itself, and it is why the trim inside composeOverallComment is not a false-positive source - the same function runs on the same parts at both ends. (2) MY CITATION WAS WRONG: composeOverallComment is at grade/types.ts:28-37, not :154-162. I crossed it with assessment-row.ts:154-162, which is applyAssessmentResult''s spread. RESUBMIT_NOTICE at :15 is correct. THE BIAS, and it is deliberate: byte equality means ANY drift falls to the INCLUDE branch, so the predicate errs toward false positives. A false positive reproduces part of a duplication the instructor can see in the box and delete; a false negative would SILENTLY DROP instructor-authored prose from a copy headed to a student. Every relaxation considered - endsWith, whitespace normalisation, a prefix test - buys back a few rows and creates a false-negative surface where the omitted text is unrecoverable. Do not make this predicate cleverer later. TWO THINGS THE DESIGN DOES NOT FIX, both pinned as characterisation rather than hidden: an instructor edit to strengths or improvements leaves overallComment stale (editAssessmentField at assessment-row.ts:120-129 does not recompose), so that row still copies superseded text - BYTE-IDENTICAL to today, not a regression, and the repair needs a per-surface mutator because the other surface''s overallComment is model-authored; and a diverged comment loses the resubmit notice from the copy, also byte-identical to today, because the instructor overwrote the only place the notice was stored. AND THE HONEST ADMISSION THAT MATTERS FOR THE WAVE: all seven landed joinFeedback cases (grading-row.test.ts:62,71,78,83,88,100,104) are BYTE-IDENTICAL under the new derivation, so that file is not edited and serves as the no-op-mutant control - which also means NOTHING CURRENTLY IN src GOES RED IF THIS FIX IS REVERTED. Every bit of the evidence comes from the one new file. DERIVATION CHECKED 2026-09-20: DEFECTIVE, 4 blockers. FOUR RULINGS, taken not asked. RULING 3 - WIDEN joinFeedback TO AssessmentFeedback. The required function prop DOES NOT COMPILE as scoped: under strict, joinCopyText typed (feedback: AssessmentFeedback) => string cannot accept a (row: GradingRow) => string, measured TS2322 by an isolated probe run outside the project. Of the two escapes, take the widening, NOT the method-shorthand bivariance hole. Reason it is not a concession: joinFeedback reads ONLY strengths, improvements and overallComment, every one of them on AssessmentFeedback - so AssessmentFeedback is its HONEST parameter type and GradingRow was always wider than the function needed. The type barrier this loses (today a snapshot row cannot be passed to joinFeedback) was ACCIDENTAL, not designed, and ruling (c) already established the prop cannot prevent substitution anyway - the seam pin is the instrument for that, and it must name SnapshotResultCard passing joinAssessmentFeedback and GradingTableRow passing joinFeedback. RULING 4 - THE VERIFY CARRIES ITS OWN CONTROL. The single-path verify did not execute the artifact''s own designated no-op-mutant control (grading-row.test.ts''s seven cases), which is a pass condition narrower than the blast radius - the same class round 2 disposed, so it goes to disposal now rather than another revision. The resolution reconciles it with L14 instead of trading one off against the other: THE NEW FILE DUPLICATES THE SEVEN CONTROL CASES. Duplication is already this repo''s rule (no cross-test-file imports - importing re-runs the other file''s describe blocks), so a single path becomes both runner-decidable AND self-controlled. State in the row that the verify is NECESSARY, NOT SUFFICIENT: tsc, the full suite and assessment-shared''s structure test are wave-gate concerns and the closure is verify AND wave gate, never verify alone. RULING 5 - DELETE THE EMPTY-overallComment GUARD. It is provably unobservable: a blank overallComment cannot equal compose(S,I,RESUBMIT_NOTICE) because the notice is non-blank, and equals compose(S,I,"") only when S and I are both blank, in which case the fall-through returns the blank value and joinFeedback''s own nonblank filter drops it. Every input yields the identical string with the line deleted, so a deletion mutant of it CANNOT go red - a guaranteed-green false control. Delete it rather than keeping it as defensive decoration. RULING 6 - THE FORBIDDEN-WORD SCAN APPLIES TO THE EDITED FILE, NOT THE TEST''S PLACEMENT. The artifact reasoned about it for the new test file, where it is irrelevant. AssessmentFeedbackFields.tsx is IN the scanned directory and IN owns, and the string SnapshotResultCard contains "shot" at index 4 - so the doc comment justifying the new prop by naming its second caller turns that test red. Describe the contract without naming either surface. MEASURED BY ME, closing the third blocker: the claim that only markLate.wiring.test.ts reads those component files is FALSE, and the instrument is the reason. A by-name grep finds 6 lines, mostly comments. But grep -rln "walkTsxFiles\|readdirSync" --include=*.test.ts src returns TWENTY test files that WALK directories, every one of them invisible to a by-name search - including confirmArmButtons.test.ts:186-197, which reads every .tsx under src/app/components and therefore reads all three files in this owns list. Its conclusion survives (it looks for onBlur paired with a consequence aria-describedby, which this change does not add) but the enumeration does not. DURABLE METHOD RULE: to find every test that reads a source file, grep for the DIRECTORY WALKERS as well as the by-name reads; the fourth recurrence of an enumeration asserted as a set. ALSO CARRY: (a) the claim that leading whitespace on strengths is the sole false-negative exception is FALSE in both directions - when either equality branch fires the copy emits RAW parts while overallComment holds the TRIMMED join, so the copy is a strict SUPERSET and nothing is lost. An implementer "closing" that exception by trimming would change output for such rows, and no landed fixture carries leading whitespace, so it would ship silently. (b) grading-row.ts is CLIENT-BUNDLED and the design makes it value-import @/lib/grade/types - the exact specifier shape gradingResultsHelpers.test.ts:858-862 BANS after a measured next build break. Measured safe here, because grade/types.ts has exactly one import and it is type-only, but the artifact never measured it and no guard covers this directory. Measure it in the build gate and say so. (c) the wire round-trip does NOT disturb the predicate - serialization passes the three strings through with a typeof check and NO trim, so a reloaded row still byte-matches its own recomposed composition. Had it trimmed, every reloaded row would fall to the diverged branch and the fix would be inert in the dominant usage. Worth recording so nobody re-derives it. (d) the sibling surfaces recompose on edit (gradingResultsHelpers.ts:333, repoGradesCellEdits.ts:196), so the obvious future "make editAssessmentField recompose like its siblings" change would silently redefine what diverged means here, and nothing in owns would catch it. |
| assessment-shared: row restore and feedback composition | A11 | unscoped | Snapshot grading always ships an EMPTY Strengths box, while the text that belongs in it sits in Overall comment. Found while scoping A10, not reported by the owner - so nobody has confirmed how it reads in the app, and that is the first thing to check. | - | - | A10 | MEASURED 2026-09-20, directly, so a scoping pass need not re-derive it - and the measurement REFRAMES the row. The Strengths box is not occasionally empty; NOTHING CAN EVER FILL IT on this surface. Three links, each grepped: (1) src/app/components/assessment-shared/AssessmentFeedbackFields.tsx:135-142 renders an editable field labelled Strengths bound to feedback.strengths, shared by the recording and snapshot surfaces; (2) src/app/components/snapshot-grading/useSnapshotGrade.ts:264 sets strengths to a hardcoded empty string - the only occurrence of the identifier in that file; (3) src/app/components/snapshot-grading/snapshot-parse.ts:146-147 and :222-223 show the snapshot model contract has exactly TWO prose fields, overallComment and improvements - strengths is not parsed, because the prompt never asks for it. So the praise the row says belongs in Strengths is in overallComment BY CONSTRUCTION, not by an accident of routing. THE FIX IS A CHOICE, not a repair, and belongs to the owner rather than a default: (a) extend the snapshot prompt and parser with a strengths field so the box is populated - the fuller build, and consistent with the recording surface; or (b) stop rendering the Strengths box here, which means the SHARED component needs a per-surface way to omit a field. Note (b) collides with A10, whose scoping proposes giving that same shared component an optional per-surface prop - sequence them, or let whichever lands first establish the per-surface seam. | A10 scoping pass residual R-1, 2026-09-15 | MEASURED 2026-09-15, one line and unambiguous: useSnapshotGrade.ts:195 hardcodes strengths: "" in the applyAssessmentResult call, alongside improvements: result.answer.improvements and overallComment: result.answer.overallComment. So on a fresh snapshot grade the Strengths field is empty BY CONSTRUCTION, not because the model declined to fill it. WHY THIS IS THE SAME ROOT AS A10 AND STILL A SEPARATE ITEM: A10's scoping pass established that overallComment carries two incompatible meanings behind one shared type - DERIVED on grading-recording (grading-feedback-prompt.ts:150-153 sets strengths = parsed.overallComment and composes overallComment from the parts) and AUTHORED on snapshot-grading (snapshot-parse.ts:222 parses it straight off model JSON). This item is that split seen from the snapshot side: the model's what-went-well prose lands in overallComment and the box named for it stays blank. Deliberately NOT folded into A10, whose brief is the duplicated copy - but the two will touch the same type, so whoever works this reads A10's artifact first. WHAT MUST BE ESTABLISHED BEFORE A FIX, and it is a product question, not a code one: does the snapshot model's response actually carry a separable strengths value that is being dropped, or does it emit ONE prose block that the recording surface happens to re-label as strengths? Read snapshot-parse.ts and the snapshot prompt. If it is one block, then the recording surface's strengths = parsed.overallComment is the re-label and the honest fix may be to stop showing an empty box rather than to invent a value for it - do not default that. UNVERIFIED IN THE APP: this repo renders no component under vitest, so how the empty box actually reads to an instructor is a reading claim. The owner should look at a fresh snapshot grade before anyone designs around it.ORCHESTRATOR RULINGS 2026-09-15, after the scoping check returned DEFECTIVE (6 blockers, 5 major, every blocker a NEW class) with its stopping point at RULINGS. Per iteration-caps.md routing the orchestrator rules and the author is NOT re-dispatched. RULING 1, SEQUENCING, and it REVERSES what the scoping artifact concluded: A11 IS BLOCKED BY A10, and the block runs through the path the artifact called independent. The artifact ran the A10 coupling in one direction only - it asked what A10 does to A11 and never asked what A11 does under A10 - and concluded that removing the box (path b) was independent while populating it (path a) was not. Executed in the other direction against the real function, both halves invert. A10's candidate fix on file is to give AssessmentFeedback its own resubmitNotice field and join [strengths, improvements, resubmitNotice]; assessment-row.ts:109 today returns [row.strengths, row.improvements, row.overallComment] filtered. On the SNAPSHOT surface strengths is the empty literal and no resubmitNotice exists anywhere (grep -rn "resubmitNotice\|RESUBMIT_NOTICE" over both directories exits 1, against a canary of overallComment over assessment-row.ts which hits 3 times), so that join yields IMPROVEMENTS ONLY and the model's authored overallComment disappears from every snapshot Copy feedback. So A10's candidate fix, applied as written, silently breaks a second surface - recorded on A10 as well, because that is A10's defect to carry and not A11's. RULING 2, ADMISSIBILITY OF PATH (b): the per-surface prop it requires is FORBIDDEN by an executing test, and the scoping cleared that test on the wrong token. assessment-shared.structure.test.ts:24-32 is a raw-substring forbidden-word scan (source.includes at :50, so comments count) over every file in assessment-shared/, and its word list contains "snapshot". The scoping searched that file for "strengths", found nothing, and marked it CHECKED-SAFE - an identifier-shaped search used to clear a semantic invariant, which is this repo's own named trap applied to an owns list. THE RULE, and it stands beyond this item: assessment-shared may not learn which surface it is rendering for. Any per-surface difference is expressed by the CALLER passing different data, never by the shared component branching on a surface name. Path (b) AS SCOPED is therefore inadmissible; a caller-side form of it is not. RULING 3, THE OWNER STEP IS BACK: the artifact demoted this row's own stated first check - the owner looks at a fresh snapshot grade - to a footnote, on the strength of a claim that AssessmentFeedbackFields renders unconditionally. That claim is FALSE. SnapshotResultCard.tsx:77,92 gates the entire block behind hasResult (row.state === "ready" or "failed"); the artifact read lines 170-198 and never followed the close upward. The ready-path conclusion survives, but the reasoning that retired the owner check does not, so the owner check stands. THE FINDING THAT REPRICES THE WHOLE ROW, and the reason the owner question could NOT be asked as the artifact phrased it: THE STRENGTHS FIELD IS NOT DEAD. It is instructor-editable and persisted - AssessmentFeedbackFields.tsx:137 wires onEditField, SnapshotGradingPanel.tsx:654 threads editAssessmentField, and snapshot-row-serialization.ts:72,146,226 round-trips strengths through ta-snap-table. So removing the box DELETES A WORKING CAPABILITY (the instructor writing their own strengths) and strands any previously-typed value in storage, still flowing into Copy feedback while invisible and uneditable - the exact hazard A10's own note names and rejects. The honest framing is not "invent a value or remove a dead box", it is POPULATE A WORKING FIELD OR DELETE A WORKING ONE, and the artifact put (b) to the owner as costless. A reduce-to-owner disposal is only legitimate if the question is fair, so the question is re-phrased and re-asked after A10 lands. ONE MORE TRAP RECORDED FOR WHOEVER BUILDS PATH (a): the verify the artifact proposed CANNOT FAIL for the defect path (a) creates. assessment-row.test.ts:49,54,59 exercise joinAssessmentFeedback against hand-written fixtures and never compose a snapshot row, so path (a) can ship with the Strengths box and the Overall comment box showing byte-identical text side by side - violating prompts.ts's own rule that the two boxes must not repeat each other - while lint, tsc, next build, the full suite and every structure test stay green, because nothing here renders a component. Any build of path (a) needs an oracle that composes a snapshot row end to end. ALSO UNCLASSIFIED BY THE SCOPING and owed to any future owns list: snapshot-row-serialization.ts:72,146,226, assessment-row-store.test.ts:173-185 (an exact-key-set oracle listing strengths among ten wire fields, which any field change trips), and grading-rows.ts:26,156 - a non-test hit of the scoping's OWN quoted grep, omitted from the output it presented as complete. ORCHESTRATOR RULING 2026-09-20, taken rather than asked, under the owner''s standing instruction to keep going and their standing preference for the fullest build. BOTH DEAD FIELDS ON THE SHARED AssessmentFeedbackFields COMPONENT ARE POPULATED, NOT REMOVED. A11: extend the snapshot prompt and parser with a strengths field so the Strengths box is filled, matching the recording surface, rather than teaching the shared component to omit a field. A10: the recording surface''s Overall comment box stays and is folded into copyText, rather than dropped or knowingly left dead. RATIONALE, and it is one rationale for both: the alternative in each case is a per-surface field-omission mechanism on a component two surfaces share, which is strictly more machinery than filling the field, and it would have to be built TWICE or built once and coupled across two in-flight items. Populating is also the direction the repo already went on the sibling surfaces - gradingResultsHelpers.ts and repoGradesCellEdits.ts both carry all three parts and derive the composed field. THE COLLISION IS THEREBY DISSOLVED: A10 and A11 no longer both need a per-surface omission seam, so they can be built in either order. If the owner disagrees, the cost is one message and the omission seam is still available. SCOPED AND CHECKED 2026-09-20: DEFECTIVE, 4 blockers. FOUR RULINGS, taken not asked. RULING A - BUILD THE CODE-HELD SIGNAL; DO NOT ACCEPT A RESIDUAL. The check is right that the scope as written has NO code-held property: build it exactly and every gate goes green while two failure modes stay silent and indistinguishable - the model omits strengths, degrading to "" and reproducing TODAY''S BUG with no signal; or the model emits strengths AND still summarises praise in overallComment, reproducing A10''S DEFECT on this surface via A11''s own fix, which is the very outcome used to reject the other fork. That is the mechanism A8 was re-chunked for, so it does not get a residual here. TAKE THE MITIGATION THE CHECK NAMED AND THE ARTIFACT DECLINED: DISTINGUISH ABSENT FROM EMPTY AT THE PARSE BOUNDARY and surface it. The pattern already ships on this very surface - snapshot-row.ts:114-126 carries shotReports with a "Not read"/"Partly read" badge "named above the score, never buried", and an optional imageFallbackNote. A strengths-missing notice is the same shape. That converts the guarantee from "the model obeyed" to "the code knows and says so", which is what every other row in this queue has been held to. RULING B - THE COUNTER-CLAUSE MUST RANGE OVER ALL FOUR LOCI, NOT TWO, AND MUST EXTEND THE PAIRWISE BLOCK TO THREE FIELDS. Measured directly: praise-routing is instructed in FOUR places, not the two the artifact named. prompts.ts:68 is the strongest and was missed - it is the JSON SCHEMA DESCRIPTION inside the shape the model copies, reading "what the student did well, and for each deduction the rubric area and specific reason". Then :90 summarize strengths, :96 "Praising work in overallComment and then advising in improvements is the INTENDED division", and :99 the 2:1 ratio anchored to overallComment only. Worse, the CRITICAL non-duplication block at :92-96 is PAIRWISE BY CONSTRUCTION - "both fields", "the other field", "SEPARATE, side-by-side boxes" - so a THIRD prose field falls entirely outside its protection, and the composed prompt would forbid overallComment/improvements duplication while saying nothing about strengths/overallComment duplication. A counter-clause written against :90 and :99 alone leaves the schema line and the intended-division line standing, and buys nothing. RULING C - A11 IS SEQUENCED BEHIND A10 AND OWES AN ASSERTION ON THE COPY IT CHANGES. The disjointness claim is true on file sets and FALSE on behaviour. SnapshotResultCard.tsx passes joinCopyText={joinAssessmentFeedback}, and assessment-row.ts:108-109 joins three fields filtering blanks - so today every snapshot row copies TWO blocks because strengths is always "", and after A11 it copies THREE, praise first. That is a user-visible change to a surface A10 owns, whose verify asserts on that exact wiring. The row''s blocked_by already names A10; honour it, and add an assertion covering snapshot copy output with a NON-EMPTY strengths, which nothing in the proposed owns covers. RULING D - THE DEAD-SHIP GUARD IS TWO GUARDS, NOT ONE, and useSnapshotGrade.wiring.test.ts JOINS owns. Confirmed by the check: adding a required field to SnapshotGradeAnswer does NOT redden tsc at useSnapshotGrade.ts:264, because AssessmentResultInput already requires strengths: string and the literal "" satisfies it - so prompt, parser and mapper can all land with the hardcode surviving, every gate green. A source-text pin that useSnapshotGrade.ts contains no empty-string strengths literal is NOT sufficient on its own: an implementer who writes the empty string INSIDE the new mapper passes tsc and that pin. So: (1) a unit oracle on the mapper itself, asserting a non-empty answer strengths reaches the result, AND (2) the source-text pin. Measured: useSnapshotGrade.wiring.test.ts today contains ZERO references to strengths, improvements, overallComment or applyAssessmentResult, so nothing in the repo pins that call site''s field mapping - it is the only wiring instrument over this seam and it must be owned. CARRY ALSO: the claim that snapshot-grade-prompt.test.ts is inert in both directions is overstated - measured 28 expect( and 16 it(, not the 14 asserted, and :30-34 and :129/:137 ARE sensitive to prompt composition. The OPERATIVE claim survives and should be restated precisely: no assertion in that file pins CITATION_AND_REPORTING_CONTRACT''s field set or the composition array''s membership, so adding or forgetting a strengths bullet is invisible to it. CORRECTIONS TO THIS ROW''S OWN SETTLED BLOCK, both mine: the Strengths TextField is at AssessmentFeedbackFields.tsx:141-150, not :135-142 - my cited range ENDED at the label line and EXCLUDED :143 value={feedback.strengths}, the binding the whole claim rests on. And this note elsewhere still says useSnapshotGrade.ts:195; the hardcode is at :264. ALSO PRE-EXISTING, in a file A11 edits: snapshot-grade-prompt.ts:19 cites the generosity sentence as prompts.ts:70; it is :84. Not A11''s defect, but it sits in the header block the implementer reads while adding the counter-clause, and the artifact leans on that block as its precedent. |
| assessment-shared: row restore and feedback composition | G2 | verification | A restored assessment list is seeded from a localStorage-reading useState initializer with NO mount effect, on an SSR'd surface - so per this repo's own documented rule it never shows its restored value on reload. RE-MEASURED 2026-09-14 and the original framing understates it TWICE: the seed is not snapshot-grading's, it is in the SHARED hook useAssessmentRowStore (src/app/components/assessment-shared/useAssessmentRowStore.ts:96-98), so it affects every grading surface built on it; and "recovers by re-render" contradicts what this repo states in two places about this exact pattern. | - | - | - | MEASURED 2026-09-14. THE SEED: useAssessmentRowStore.ts:96-98 is useState<R[]>(() => typeof window === "undefined" ? [] : deserializeAssessmentRows(window.localStorage.getItem(STORAGE_KEY_TABLE), codec)) - a localStorage read inside the INITIALIZER, and grep -c useEffect over that file returns 0, so there is no mount effect to correct it. THE RULE THIS BREAKS IS THE REPO'S OWN, stated in two independent places: SnapshotGradingPanel.tsx:167-172 says verbatim that "A localStorage-seeded useState initializer never shows its restored value on an SSR'd surface - it needs a mount effect", citing LectureScriptPanel.tsx:9-17/46-60, and the same panel FIXES it correctly for instructor instructions at :174-186 using the async-IIFE-plus-cancelled-flag idiom. So the panel knows the rule, applies it to one piece of state, and consumes a shared hook that does not. BLAST RADIUS: grep -rln useAssessmentRowStore over src/ excluding tests returns 7 files, including grading-recording/useGradingRows.ts as well as snapshot-grading - so this is not one surface. A SECOND instance of the same pattern sits at snapshot-grading/useSnapshotShots.ts:100, useState<SnapshotRole>(() => readArmedRole()), which reads ARMED_ROLE_KEY with no mount effect either. UNVERIFIABLE HERE and stated as such: no component is rendered by any test in this repo, so "the list does not appear on reload" is a READING claim resting on the repo's own twice-stated rule, not on an observation. An owner reload would settle it in seconds and should be the first step. | 82f5bde | MOVED TO VERIFICATION 2026-09-14, deliberately, after being measured. It had been given owns and verify while its own evidence said the first step is an OWNER OBSERVATION - that combination would have let an agent start building before the cheap check ran, so the fields are cleared and the row sits where it belongs. THE OWNER STEP, which takes seconds: open a grading surface that has completed assessments, reload the page, and say whether the list still appears. EITHER ANSWER IS USEFUL AND THEY LEAD OPPOSITE WAYS. If the list does NOT appear, this is a live user-visible defect in a SHARED hook with seven importers, and the fix is a known in-repo pattern (below) - re-scope it actionable and build it. If the list DOES appear, then this repo's own twice-stated rule is WRONG, and that is the more valuable finding: SnapshotGradingPanel.tsx:167-172 and LectureScriptPanel.tsx:9-17/46-60 both assert it as settled, and one of them added a mount effect on its strength. Correcting a false rule two files rely on beats fixing a symptom that does not exist. THE FIX, if it is needed: move the localStorage read out of the initializer into a mount effect using the async-IIFE + cancelled-flag idiom (SnapshotGradingPanel.tsx:174-186 is the working example in the same directory; eslint react-hooks/set-state-in-effect requires setState be reached only after an await). Do it IN THE SHARED HOOK, not per consumer - the finding is that one seed has seven importers - and fix the armedRole seed at useSnapshotShots.ts:100 in the same wave, since splitting them would leave a near-duplicate row. Original scoping note follows. SCOPED 2026-09-14. The fix shape is already demonstrated in-repo twice, so this is a pattern application rather than a design problem: move the localStorage read out of the initializer into a mount effect using the async-IIFE + cancelled-flag idiom (SnapshotGradingPanel.tsx:174-186 is the working example in the same directory; eslint react-hooks/set-state-in-effect requires setState to be reached only after an await). DO IT IN THE SHARED HOOK, not per consumer - the point of the finding is that the defect is one seed with seven importers. Also fix the armedRole seed at useSnapshotShots.ts:100 in the same wave, since it is the same pattern in the same feature and splitting them would leave the queue with a near-duplicate row. CHEAPEST FIRST STEP, before any code: the owner reloads a page with completed assessments and says whether the list appears. If it DOES appear, this row is wrong and the repo's twice-stated rule needs correcting instead - which would be a more valuable finding than the fix. |
| Discussion replies graded as independent posts | A8 | unscoped | The discussion grader treats a student's REPLIES as independent posts. Owner, 2026-09-15, with a screenshot of the submission view: one student's three contributions to a Student Introductions topic are listed as three peer items - their actual introduction, plus two replies addressed to other students ("@Seth Vander Vorst", "@Luke Demmel") - under a heading that calls all of them the assignment's submissions. A reply to a classmate is not a second introduction, and grading it as one both misreads the work and inflates whatever a rubric counts. | - | - | - | Owner screenshot plus a direct read of src/lib/canvas/discussions.ts:106-152, 2026-09-15 | owner request, 2026-09-15 | OWNER DECISION 2026-09-15, asked and answered: REPLIES ARE GRADED SEPARATELY, AGAINST DIFFERENT CRITERIA. Not excluded, not merely labelled. That is the reading most discussion rubrics already use - an initial post scored on substance against the prompt, and replies scored on engagement with a classmate - and it makes the current behaviour a real scoring error rather than a presentation one: a reply is being measured against criteria written for an introduction it was never trying to be. WHAT THE DECISION IMPLIES, for whoever scopes this. (1) THE RUBRIC GAINS A SECOND AXIS, and that is the design question: does the instructor supply two rubrics, or one rubric with a reply section, or does the app split a single rubric by convention? Do not default it - it decides the whole surface. (2) THE SCORE IS NO LONGER ONE NUMBER per student per discussion, which touches whatever posts to Canvas: check whether the grade path can carry two component scores or whether they must be combined before posting, and if combined, by what rule and whether the instructor sets it. (3) contributionCount (discussions.ts:152) sums posts and replies into ONE NUMBER and must become two, or the separation cannot be counted. (4) A STUDENT WITH NO REPLIES and a student with no initial post are now different failures and must read differently. (5) The UI in the owner's screenshot lists every contribution as a peer item with no indication which is the initial post - under this decision that is not cosmetic, it is the surface where the two scores have to live. MEASURED BEFORE FILING, and the finding narrows the search usefully: THE DATA LAYER ALREADY KNOWS. DiscussionPost carries isReply (discussions.ts:13-14) and parentUserId (:15-16); extractDiscussionActivity splits a student's work into initialPosts and replies (DiscussionActivity, :19-22), computing isReply from thread depth at :61 and pushing to the right bucket at :69. So the distinction is captured correctly at the source and something downstream flattens it. TWO CANDIDATES, both visible at discussions.ts:141-152. (1) The text handed to grading is built by concatenating [...initialPosts, ...replies] with each segment prefixed "Post: " or "Reply: " and joined by a --- separator - so the label IS present, and the question is whether the grading prompt is told what those labels MEAN or whether the model just sees three chunks of prose. (2) contributionCount is initialPosts.length + replies.length, ONE NUMBER with the distinction summed away - so anything counting contributions cannot tell a student who wrote one post and two replies from one who wrote three posts. THE SCOPING PASS MUST ALSO CHECK THE UI, which is what the owner actually saw: the screenshot is a submission-list surface that renders each contribution as a peer item with its own timestamp, and nothing in it says which one is the initial post. That may be a separate defect in the same family, and if it is, say so rather than fixing only the model-facing half. WORTH ASKING EARLY, because it changes the fix: does the owner want replies EXCLUDED from grading, graded SEPARATELY against different criteria (most discussion rubrics score the initial post and the replies on different lines), or merely LABELLED so the model and the instructor can tell them apart? Do not default this. SECOND OWNER DECISION 2026-09-15, closing implication (1), the one the scoping pass was told not to default: ONE RUBRIC WITH A REPLY SECTION - "one rubric with a reply section". Not two instructor-supplied rubrics, and not an app-side split of a single rubric by convention. So the rubric input gains an explicit reply section the instructor authors, the initial post is scored against the main body and the replies against that section, and the app never has to guess which criteria belong to which. Implications (2) through (5) are unchanged and still open. DISPOSAL 2026-09-15, under iteration-caps.md cap 2 and the REPEAT-class rule. A8's scoping had two revisions and two fresh checks; round 2 returned five blockers of which THREE were REPEATs of round 1's own classes (two false-absence, two pass-condition-too-narrow). Per the routing table a REPEAT goes to disposal NOW, and the orchestrator failed to do that for a round. No further revision of this artifact is legal. Recorded here so the scoping does not restart from zero. (a) RELOCATE - the UI half. Two candidate surfaces were found by render-shape search after an identifier-shaped search wrongly concluded none existed. Candidate 1, recording/DiscussionReplyTable.tsx, DOES render the distinction (a Reply badge at DiscussionReplyRow.tsx:629 and "Replying to X" at :645-646) and DOES render a per-item timestamp (formatCapturedTime at :601 under a "Captured" header) - so the artifact's stated reason for excluding it was FALSE, though the exclusion itself probably holds on other grounds: it is the multi-student reply-DRAFTING tool, one row per post across students, and no heading calls anything submissions. Candidate 2, the screen-capture submissions list, is live and lives entirely in A9's directory. OWNER ASKED AND ANSWERED "not sure" on which screen was photographed, so this cannot be settled by more argument: RECEIVER is whoever next opens those two surfaces in the running app, OBLIGATION is to say which one the owner saw. Until then the UI half does not ship. (b) REDUCE - one question for the owner, and it gates A8-2: ON THE EMBEDDED PATH, WHAT CRITERIA DO REPLIES GET once they stop borrowing the initial post's? Implemented as scoped, A8-2 routes 4 of the 7 union-read signals to the initial-post bucket, after which reply CONTENT is read by exactly one signal (mentions_peer, a first-name substring match at discussion.ts:158-165). A student who writes two substantive, sourced, question-asking replies would earn credit for that content NOWHERE - where today they at least earn Quality-proxy credit. That is graded-separately turning into graded-against-nothing, and it is strictly worse than the defect. A8-2 MUST NOT SHIP until this is answered. (c) RESIDUALS, each with owner/instrument/step in the artifact: the four-column pass condition, the blanket swap guard that contradicts the deliberate distinct_days ruling (Timeliness is invariant under a content swap BY CONSTRUCTION, so the guard goes red on a CORRECT implementation and must be restricted to three columns), and M-b's construction point - a shared exported constant makes the course-intel literal divergence unrepresentable and is enforced by tsc, which is strictly stronger than the source-text test the artifact proposed. (d) ORCHESTRATOR RULING, mine, correcting the chunking I own: A8-1's wiring file is src/lib/canvas/discussions.ts, NOT src/lib/grade/extraction.ts. canvasWorkToEntry never reads work.discussion; discussions.ts:144-146 fills work.text and extraction.ts:160-170 merely reads it. As filed, A8-1 could add the module, its test, the barrel line and the prompt clause, never rewire discussions.ts, and pass lint, tsc, next build, vitest and every structure test while the model still receives the old flat text - the assignment-must-include-the-wiring-file trap with the wiring file misidentified. A8-1 also needs one end-to-end assertion on work.text for a discussion fixture; nothing in the repo asserts on that concatenation today. SCOPED AND CHECKED 2026-09-20; the check came back DEFECTIVE and its findings are the durable part. (1) A THIRD GRADING SURFACE EXISTS AND IS THE ONE PHOTOGRAPHED. The scoping concluded neither of two surfaces can list three peer items for one student and floated that the screenshot was Canvas''s own UI. That is the SECOND time this row has produced that conclusion - docs/loop/traps-search.md already records the first, and src/app/actions/grading-submission-extract.ts:60-79 carries a source comment dated 2026-09-15 written because of it. The surface is the SCREEN-CAPTURE grading path: grading-extraction-prompt.ts:77 names "a discussion-style response" as a submission; grading-submission-merge.ts:301-324 isSameSubmission needs a name match AND text similarity, so one author''s introduction plus two replies stay three rows (the bias is pinned deliberately at grading-submission-merge.test.ts:272); GradingTable.tsx:167 renders "Showing N of M submissions" over them. ANY FUTURE PASS MUST SEARCH BY WHAT THE SURFACE HOLDS, NOT BY THE CANVAS-DISCUSSION TYPE NAMES - both wrong conclusions came from grepping the data layer. (2) THE REFRAME, which may make the scoped fix unnecessary: a deterministic reply-aware discussion grader ALREADY SHIPS. embedded-grader/discussion.ts:88-102 computes initialCount, replyCount and peerReplyCount separately and peerReplyCount already excludes self-replies; grading.ts:774-783 routes graded discussions to buildDiscussionRubric /gradeDiscussion WHEN THE PROVIDER IS embedded. So the AI provider is the one that flattens, and the instructor''s provider choice silently decides whether replies are understood. A8 may be a ROUTING defect, not a prompt defect. (3) THE SCOPED AI-PATH FIX IS A HOPE, NOT A GUARANTEE. It ships a renderer plus one sentence appended to buildSystemPrompt''s rule list - competing with ~30 bullets that treat the submission as one blob, including prompts.ts:83-86''s "award full points absent an explicit violation" and grade-generously rules, which make reply-padding HELP the student. It can pass lint, tsc, next build and every test while changing zero grades. (4) A FOURTH ROUTE the scoping never named: grading.ts:619/:636 gradeOneSubmissionAction calls gradeEntries directly, bypassing gradeCanvasUrl, and its work comes from submission-detail.ts:120 where a graded discussion''s body arrives with NO Post/Reply labels and no discussion field at all - so there the defect is worse and the scoped fix changes nothing. BLOCKED ON THE OWNER for three product calls, batched: which screen was photographed; whether a reply with no rubric reply-section is scored against the main criteria, against an app-authored reply standard, or excluded and disclosed; and whether graded discussions should route to the deterministic engine regardless of provider. OWNER DECISIONS 2026-09-20, all three, and they resolve the block. (1) SCREENSHOT: "canvas split out the initial posts from the replies, but this app should recognize the replies as being replies and not mark them as original posts (like it did with the defect i reported)". So CANVAS DELIVERED THE DATA ALREADY SEPARATED and THIS APP re-merged it and labelled replies as original posts. Both scoping passes that blamed Canvas''s own UI were wrong in the same way twice; the defect is this app''s recognition of replies, not Canvas''s presentation. (2) REPLY SCORING when the rubric has no reply section: EXCLUDE the replies from the initial-post criteria and DISCLOSE the gap in the feedback. Do not invent an app-authored reply standard, and do not keep scoring them against the main criteria. (3) ROUTING: DO NOT force graded discussions to the deterministic embedded engine. The provider choice stays authoritative; FIX THE AI PATH. WHAT THAT MEANS FOR THE SCOPE, given the check''s B2: a renderer plus one prompt sentence is NOT sufficient, because the owner chose exclusion, and exclusion is a code decision - the deterministic half. The clause competes with ~30 bullets that treat the submission as one blob (prompts.ts:83-86 awards full points absent an explicit violation, and the grade-generously rules make reply-padding help the student). A re-scope must carry a GUARANTEED-class component: what the code holds regardless of what the model returns. STILL OPEN AFTER THESE ANSWERS, from the check: the fourth route at grading.ts:619/:636 (gradeOneSubmissionAction bypasses gradeCanvasUrl, and submission-detail.ts:120 gives it a body with no labels and no discussion field), and the screen-capture path, which by the owner''s answer is in scope because it is where replies get marked as original posts. RE-SCOPED 2026-09-20 against the three owner decisions; check in flight. THE DURABLE FINDINGS. FIVE AI-REACHING ROUTES, not one - "fix the AI path" is ambiguous and anyone reading it as gradeCanvasUrl misses three: (a) the Canvas-API path; (b) SCREEN CAPTURE; (c) grading.ts:597-641 gradeOneSubmissionAction, which bypasses gradeCanvasUrl entirely and whose submission-detail.ts:120 body arrives with no labels and no discussion field; (d) the external "other" engine, which at canvas/submissions.ts:182-183 writes the merged intro-plus-replies blob to a file literally named _post.txt - the most literal instance in the tree of the app marking replies as an original post, and named in no prior pass; (e) the screen-capture GRADING route, grading-submission-grade.ts:69 -> grading-feedback-prompt.ts:153, which reuses buildSystemPrompt. WHICH SURFACE IS THE SCREENSHOT, settled by a CONSTRUCTION argument rather than a grep - this is what the two earlier wrong conclusions lacked: routes a/c/d/e each produce exactly ONE ROW PER STUDENT per assignment, so none can list three peer items; (b)''s row count is per-extracted-text, because ExtractedSubmission is {name,text} (grading-submission-merge.ts:54-57) with no kind field, isSameSubmission (:301-324) needs a name match AND text similarity so three different texts by one author stay three rows, and nothing groups by name (:395-428). CORRECTION TO THIS ROW''S OWN GLOSS: "this app re-merged it" is true of (a),(d),(e) and FALSE of (b). Route (b) never merges - it fails to merge OR label, presenting three contributions as three peer submissions. A receiver building from the gloss would hunt for a merge that is not there. THE GUARANTEED COMPONENT, which the prior scoping lacked and which is why that one was called a hope: G1 - the model NEVER RECEIVES THE REPLY TEXT. canvasWorkToEntry builds initialPostContent from work.discussion.initialPosts, NEVER by string-filtering work.text, and engine.ts:211-215 selects it when the rubric has no reply section, so reply prose inside the scored content is UNREPRESENTABLE. That is what defeats prompts.ts:84-85 (grade generously; award full points absent an explicit violation) and the other 22 rule bullets - no bullet can act on text that is not in the request body. G2 - the disclosure is composed by CODE via composeOverallComment, the same mechanism types.ts:208-212 documents for resubmitNotice as never model-generated, so it survives callLlm returning an empty string. G3 - the excluded count is a TypeScript integer from replies.length. ROUTE (b) HAS NO GUARANTEE OF THAT CLASS and the scope says so rather than pretending: the input is pixels and ExtractedSubmission has no kind field, so reply RECOGNITION is inherently model-supplied. The best code-held property there is a same-name group count. Any A8-C criteria must state the reduced class explicitly. BLAST RADIUS IS COMPILER-ENFORCED: grade-result-allowlist-coverage.test.ts:47-74 makes tsc fail if a new GradeResult field is missing from ALL_GRADE_RESULT_FIELDS, and a runtime sentinel fails unless three allowlists are threaded (grading-review-rows.ts:53-58, grading-drafts.ts:120, github-grading-run-store.ts:211/:246). A WRONG-REASON TRAP the oracle must dodge: engine.test.ts:8 mocks getGeminiMaxCharsPerSubmission to 20, and at that cap truncateSubmission would strip reply text REGARDLESS of the fix, so a "reply absent" assertion would pass on broken code. The new test must mock a large cap and also assert the initial-post sentinel IS present. CONTRADICTION WITH THIS ROW''S IMPLICATION (3): contributionCount has NINE grep hits and ZERO READERS - one declaration, three writes, five test fixtures. Splitting it into two numbers would change a write-only field. Left out of all three chunks deliberately. RE-SCOPE CHECKED 2026-09-20: DEFECTIVE, 4 blockers. ORCHESTRATOR RULINGS, taken not asked. RULING A - EXCLUSION IS DEFERRED; RECOGNITION SHIPS FIRST. The scoped A8-A would make grading WORSE than the defect it fixes, and this is measured, not feared: prompts.ts:87-89 says an absent required behaviour is an EXPLICIT rubric violation and instructs the model to deduct for it and name it; grading.ts:806-813 REQUIRES non-empty assignmentInstructions on that path and passes them verbatim, and for a graded discussion those instructions ARE the discussion prompt ("reply to two classmates"). Remove the reply text and the demand for it remains, so the model is instructed to deduct for work the student actually did - where today that same text at least earns generosity credit under :84-85. Compounding it, the reply-section AUTHORING SURFACE DOES NOT EXIST yet, so the has-a-reply-section predicate answers no for EVERY rubric and the exclusion would fire universally rather than rarely. The owner''s own words asked for RECOGNITION - "this app should recognize the replies as being replies and not mark them as original posts" - not for removal. So: build recognition and labelling now; exclusion waits until a rubric can actually carry a reply section, and until the instructions shown to the model are narrowed in the same change. Exclusion without narrowing the instructions is the prior round''s disposed class recurring - content removed from a scoring path that costs the student points. RULING B - ADOPT THE EXISTING ROLE MODEL; DO NOT INVENT A WEAKER ONE. The check found a SIXTH route the enumeration missed, and it already contains the mechanism this row proposed to build: snapshot-shot.ts:27 declares SnapshotRole as a CLOSED UNION with "post" and "replies" as separate members (SNAPSHOT_ROLES :29, labels :31-38); snapshot-row.ts:358-365 buildTranscriptBlock emits "Shot N (role: replies):" INTO THE GRADING PROMPT, so the role reaches the model by construction; snapshot-row.ts:391 GradeShotSelection.excludedRoles is already a code-computed excluded-role set. That surface is ALSO a screen-capture surface, which refutes the re-scope''s claim that a pixel input makes reply recognition impossible and that the best available is a same-name group count. The screen-capture grading path adopts this role model rather than inventing a weaker mechanism. This changes A8-C''s shape and therefore the chunking, so re-chunk before dispatching anything. RULING C - A8-A MAY NOT SHIP ALONE. The guarantee as scoped lands on route (a), the ONE route that already labels contributions Post/Reply, while the surface the owner photographed keeps presenting three contributions as three peer submissions. The seam is mechanically disjoint but cuts the correctness property in half. Recognition on the photographed surface is the first chunk, not the last. RULING D - A8-B gets a LINE BUDGET: src/app/actions/grading.ts is 905 lines against LIMIT = 1000 with no ALLOWED_OVERAGE entry. 95 lines of headroom for the chunk whose whole job lives in that file. Extract first or do not start. ALSO CARRY, all measured by the check: (1) a fourth composed part in overallComment IS DESTROYED DOWNSTREAM with nothing going red - gradingResultsHelpers.ts:214 FeedbackField is a hand-written literal union with no type-level link to GradeResult, :331-335 recomputes overall from exactly three fields on any edit, and :553-566 recomputes it ON RELOAD, so a disclosure composed into overallComment evaporates the first time the instructor reloads, while tsc, lint, build, vitest and the byte-identical parity test at gradingResultsHelpers.test.ts:797 all stay green. Any disclosure must survive that, or live in a field that is not recomposed. (2) G1''s "unrepresentable" is overstated: a pseudo-entry''s previewContent reaches the model via code-run-selection.ts:143 -> utils.ts:352-381 -> engine.ts:61, so a runnable extension on a text pseudo-entry leaks it; pin the extension. (3) A SEVENTH route, grading.ts:871 gradeSubmissions(zipBuffer), is also unnamed. (4) The cardinality discriminator is internally false - route (e) IS route (b)''s grading half and shares its cardinality; the identification of (b) as the photographed surface survives on other grounds but not on that argument. (5) The "Showing N of M submissions" heading renders only when a filter is non-empty (GradingTable.tsx:168). RE-CHUNKED 2026-09-20 by the top tier; it corrected THREE OF MY FOUR RULINGS and the corrections change what gets built. (1) RULING B''S CONCLUSION IS RIGHT, ITS REASON IS WRONG, and the difference is concrete. I said the snapshot surface refutes the claim that a pixel input makes reply recognition impossible. Measured: the snapshot role is INSTRUCTOR-DECLARED, NOT MODEL-RECOGNISED - snapshot-keys.ts:24 ROLE_BY_DIGIT arms a role by keypress BEFORE capture, snapshot-shot.ts:130 setShotRole writes it, and snapshot-role-suggestion.ts:8-13 states in bold that a suggested role must NEVER become the effective role without an explicit instructor action, implemented at :83-92 where the only writer runs on a click. So the surface proves a narrower thing: a code-held closed union can exist on a pixel surface WHEN A HUMAN SUPPLIES THE VALUE. The screen-capture path sweeps MANY submissions in one capture and has NO per-submission moment before capture, so it cannot copy the arming half - it must INVERT the order: the model SUGGESTS at extraction, the instructor CONFIRMS per row. A receiver told only "adopt the role model" would try to build a pre-capture arming control for a surface with no per-submission capture moment. (2) RULING B MUST NOT BE READ AS "IMPORT SnapshotRole". Three source headers forbid that boundary crossing - snapshot-shot.ts:6-9, grading-submission-merge.ts:9-13, grading-extraction-prompt.ts:9-14. Adopt the SHAPE - closed union, Record labels, a label emitted into the prompt by construction, suggest-never-auto-apply, a pinned call-site canary - declared FRESH in src/lib/grade/, the layer grading-feedback-prompt.ts:25-33 already imports from. (3) RULING D AIMED AT THE WRONG FILE. I budgeted grading.ts at 905/1000. Under this chunking NO chunk''s work lands there - gradeCanvasUrl and gradeSubmissions are CALLED from grading.ts but the work is inside engine.ts. The real squeeze is GradingRecordingPanel.tsx at 995/1000: FIVE lines of headroom, not ninety-five, on the photographed surface''s own panel. An extraction comes first, and prefer moving a plain function and const (handleDownloadLog + currentGradingLog, :633-656, NO hook-count change) over the run-log state block, because this-repo.md records the sibling panel''s hook extraction failing lint with two React Compiler preserve-manual-memoization errors on an untouched callback. (4) THE PHOTOGRAPHED HEADING IS NOT WHERE THE CHECK SAID. GradingTable.tsx:170 renders "Showing N of M submissions" but :168 gates it on a non-empty filter, so it appears only while the instructor is typing. The UNCONDITIONAL per-item label is GradingTableRow.tsx:249 - a literal Submission span above EVERY row - and :257 aria-label "Submission from <name>". A chunk aimed at the table heading would fix a string that is usually not on screen. THE SHAPE: A8 IS A SERIAL ROW. A8-R (recognition on the photographed surface, routes b+e, 33 files) then A8-P (the LMS-connected AI paths, routes a+g, 10 files). They are FILE-DISJOINT (proven mechanically with sort \| uniq -d plus a self-intersect canary) but MUST NOT run concurrently, because A8-P imports the union A8-R invents - a directional informational coupling, so sequence rather than merge. Routes b and e are MERGED into A8-R because the seam between them cuts one property in half: a typed field with no prompt threading ships an export with no caller, and a prompt change with no typed field has nothing to read. Third time this repo has merged for that reason. FIXED IN PASSING 2026-09-20: grading-submission-grade.ts:99-105 said "NOT YET WIRED TO A PRODUCTION CALLER", measurably false - GradingRecordingPanel.tsx imports it at :105 and calls it at :566. That is byte-for-byte the stale-comment shape that already misled an A8 pass on its sibling file (grading-submission-extract.ts, corrected 2026-09-15); the sibling was corrected and this one was not. Now corrected, with the lesson in the comment. |
| Ask AI modal: persistence and chip copy | A4 | owner | Three of AskAiModal four suggestion chips are honestly chat-equivalent, and the modal ships copy that overstates them. The ideation pass concluded - soundly, and this is the directive working - that "suggest three assessments", "draft a welcome announcement" and "where is this schedule too heavy" are general teaching-practice prompts that a pasted facts blob answers equally well. But src/app/components/courses/AskAiModal.tsx:99 tells the user "Answers are grounded in this course own recorded facts", so those three ship under copy the same pass concluded overstates them. | - | - | - | L7 ideation check, 2026-09-15, which ruled that the ideation pass DEFAULTED this decision instead of escalating it; citation corrected 2026-09-15 to src/app/components/courses/AskAiModal.tsx:99 (was cited as "AskAiModal.tsx:132-134" with no directory - the grounding sentence and its file path have both moved/been corrected). | owner request, 2026-09-15 | AN OWNER CALL BY THE DIRECTIVE OWN RULE - docs/loop/leverage.md:110-121 gives three dispositions (redesign, accept the cost explicitly, reject) and says they are "decided by the human who scoped it, never defaulted by the agent", and :125 says silence is the one illegal answer. The ideation pass picked "keep and stop there", which is the one move not available. THE OPTIONS: (a) ACCEPT EXPLICITLY - keep all four chips and soften the grounding sentence so it claims only what is true; (b) RE-POINT the three chips at the fact-grounded question shapes A1 and A2 create, so the copy becomes true rather than the copy becoming weaker; (c) drop them. MY RECOMMENDATION is (b) once A1 lands - the roster, the checklist and the grades-due date are exactly the facts those three chips would need. CORRECTED 2026-09-15: I first wrote that A1 makes the grounding claim true for the first time. It does not; the sentence's four existing nouns are already emitted. A1 widens what the claim covers, and the recommendation stands on that narrower ground. Until then (a) is the honest interim and it is one sentence of copy. |
| Rich-text draft rendering and clipboard | V1 | verification | Look at the announcement preview. Its appearance is a reading claim - the preview is rendered via AnnouncementDraftSlot.tsx's markdownToHtml and styled by controls.draftPreview (RecordingControls.module.css), reviewed by eye, which is how the unstyled version passed review the first time. Check that bullets indent, paragraphs separate, and links look like links. | - | - | - | Open the walkthrough announcement panel | b3701cd | - |
| Rich-text draft rendering and clipboard | V2 | verification | Paste a copied draft into Canvas's editor. Whether a real browser accepts the multi-type clipboard write, and whether the rich flavour arrives as bold rather than asterisks, cannot be tested here: of the 9 it( blocks in src/app/components/ui/clipboard.test.ts (grep -c "  it(" ), the ones exercising ClipboardItem install a stub class, and one deliberately stubs it as undefined to pin the fallback. | - | - | - | Copy a draft, paste into Canvas | b3701cd | - |
| LLM calls against the platform invocation ceiling | G4 | unscoped | An LLM call with no timeout runs under the platform default, not 60s. callLlm takes no AbortSignal, so nothing bounds a first attempt; learning-resource-links.ts:55-66 documents a 90-100s case and says the mitigation is RETRY_BUDGET_MS = 32_000 (:121), which gates RETRY decisions and does not bound the first attempt. Compounding it: src/app/page.tsx exports NO maxDuration (grep -n maxDuration src/app/page.tsx returns nothing), and this repo already records in its own comments (src/app/api/automations/run-now/route.ts:35-37, course-intel/ask/route.ts:48-59) that Next honours maxDuration for Server Actions only at the PAGE level. So these actions run under the platform default rather than the 60s Hobby figure. When the platform kills the invocation the action's own catch never runs, so the client gets a raw transport failure instead of the {error} string the action would have returned. Reachable today via discussion-replies.ts:786 and learning-resources-generator.ts:229. Recorded because two separate G3 design passes found it and filed it nowhere. | - | - | - | RE-MEASURED 2026-09-14 and the row's framing does not survive it. THREE findings. (1) BLAST RADIUS is understated ~60x: the row names two reachable paths, but grep -rn "callLlm(" src/ excluding tests returns 124 call sites. callLlm (src/lib/llm.ts:375) takes (req, provider) and no AbortSignal, and llm.ts contains ZERO AbortSignal or timeout references - the single "signal" hit at :718 is prose about something else. So a call-level bound is a 124-site seam change, or it is optional and therefore unenforced. (2) THE PAGE-LEVEL OPTION IS NOT AVAILABLE. src/app/page.tsx is "use client" (line 1), and a client component cannot export route segment config at all - maxDuration is a server-only export. So "page-level maxDuration" is not a cheaper alternative to a call-level bound; it is not on the table without converting the page to a server component. (3) THE REPO ALREADY HAS A THIRD ANSWER and documents it: move the feature off a Server Action to a Route Handler, which CAN declare its own ceiling. Twelve route handlers under src/app/api/ already declare maxDuration, and course-intel/ask/route.ts:48-59 plus automations/run-now/route.ts:35-38 explain they did it FOR EXACTLY THIS REASON. That file also records the constraint any fix inherits: prod is Vercel Hobby, whose hard cap is 60s regardless of what a route requests, and a value above 60 makes the deployment FAIL TO BUILD. THAT LAST CLAIM IS FALSE AS STATED, measured 2026-09-15 by the N13a scoping check and re-run by the orchestrator: five route handlers in this tree declare maxDuration = 300 and main deploys - api/accessibility/route.ts:24, api/visualizer/create/route.ts:77, api/lms-export/selection/route.ts:114, api/lms-generation/deck/route.ts:53 and api/lms-generation/deck-from-capture/route.ts:55 - and visualizer/create has a test pinning the 300 (route.test.ts:317-318). lms-export/selection/route.ts:6 states the real rule outright: a route may declare more than Hobby grants, and it simply does not get it. So the ceiling is a GRANT ceiling, not a build-time validation, and any fork priced on fail-to-build was priced on a false constraint. | c25bdec-era, pre-existing | STILL UNSCOPED, and the owner decision it needs has CHANGED shape - the old either/or was a false dichotomy (see instrument). The real fork, and it is a genuine product/cost call rather than something an agent should default: MIGRATE the two named reachable features (discussion-replies.ts, learning-resources-generator.ts) off Server Actions onto Route Handlers that declare their own sub-60s ceiling - which is real work and changes how the client calls them, but follows a pattern three routes here already established and documented - OR accept the unknown platform ceiling and the raw transport failure it produces. NOTE the defect itself is NOT the slow call; it is that when the platform kills the invocation the action's catch never runs, so the client gets a transport failure instead of the {error} string the action would have returned. A bound must therefore sit BELOW the invocation ceiling for the action to return anything at all - which is why knowing the ceiling and bounding the call are two halves of one fix, not alternatives. NOT to be worked as one chunk: at 124 call sites, any callLlm signature change is a repo-wide seam and needs its own chunking. |
| Orphan-upload sweep | V6 | verification | Check whether a killed tick leaves its start-of-tick line. src/app/api/cron/sweep-orphan-uploads/route.ts:92 logs it; the sibling run-schedules/ route chose a durable DB write instead, precisely because a tick killed at the platform cap never runs finally. | - | - | - | Vercel function logs after the first timeout | c25bdec | - |
| The loop's own docs, gates and process debt | L13 | unscoped | A CRLF-blind stripComments copy silently stops defending its own assertions, and the repo has no check that the 53 duplicated copies agree. CORRECTED 2026-09-16 - THE FIRST FILING OF THIS ROW WAS WRONG and the correction is the useful part. It claimed all 53 copies were blind, from a SYNTAX filter (does the source mention a carriage-return escape). A BEHAVIOURAL probe - extract each stripComments body, eval it, feed it a CRLF fixture whose only comment is a line comment, ask whether the comment survived - says otherwise: of 53 files defining the helper, 45 strip correctly, 8 were unparseable by the probe and read CR-safe by hand, and exactly ONE is genuinely blind: src/app/components/snapshot-grading/snapshot-role-setrole-callsites.structure.test.ts, which does split on a bare line feed then replaces with a pattern lacking the multiline flag. THE RULE THE FIRST FILING MISSED: in JavaScript a multiline dollar matches BEFORE a carriage return (CR is a line terminator), so the dominant shape - 28 of 53 files - is CR-safe despite never mentioning CR. Only a form that splits on a bare line feed AND drops the multiline flag is blind. | - | - | - | Behavioural probe, 2026-09-16, run from the repo root: enumerate grep -rl "function stripComments" --include=*.test.ts src (53 files), brace-match each body out of its file, eval it, and apply it to a fixture built with String.fromCharCode(13) so no tool can materialize the escape. Result: 45 CR-tolerant, 0 blind among the parseable, 8 unparseable (inspected by hand: six use the multiline flag or an explicit non-CR character class and are safe; one is the SQL-specific stripCommentsAndDollarQuotes; one is the snapshot-grading chunk's own copy, fixed in that chunk). The single blind file was then confirmed directly. The two-direction demonstration that produced this row stands for the copy it was run on: with the directory forced to CRLF a sabotage that should fail stayed GREEN, and separately a comment carrying an unmatched open paren survived stripping and tripped a bracket-counting throw, turning a green test hard red on a clean checkout. core.autocrlf=true, no .gitattributes, and git ls-files --eol src \| grep -c "w/crlf" returns 453, so CRLF working files are the normal case here. | N15c remediation verification 2026-09-16; corrected by the A15 round-3 check the same day | WHY THIS IS STILL WORTH A ROW AFTER THE CORRECTION SHRANK IT: the defect is one file, but the exposure is that nothing detects divergence among 53 copies that the repo REQUIRES to be duplicated (cross-test-file imports re-run the other file's describe blocks, so importing is not available). One copy drifted; nothing would have caught a second. So the row is a decision, not a patch: either give these helpers a single non-test home that test files may import, or add a mechanical check that every copy agrees on a shared CRLF fixture. RELATED TO L9, NOT THE SAME: L9 is "most source-scanning tests have no comment defence at all"; this is "a defence that exists can stop working invisibly". A fix for L9 that mints more hand-rolled copies makes this row worse - sequence them. METHOD NOTE, recorded because it cost a wrong filing: a syntax filter over a helper's SOURCE does not measure that helper's BEHAVIOUR, and this repo's own rule that a measurement names its command is not enough when the command measures the wrong thing. SECOND CORRECTION 2026-09-20, and it corrects THIS ROW''S OWN RECOMMENDATION as well as every orchestrator brief this session. THERE ARE TWO FAILURE MODES, NOT ONE, AND THE FORM THIS ROW RECOMMENDED IS BLIND TO THE SECOND. Measured with a node one-liner against a CRLF fixture and a trailing-comment fixture. MODE 1, the original finding: split on a bare line-feed, then strip with a line-comment pattern carrying no multiline flag. CRLF-BLIND. One file has it. MODE 2, and this is the one the row got wrong: the anchored multiline form (caret, then optional spaces or tabs, then the comment pattern, with the m flag) is CR-SAFE BUT TRAILING-COMMENT-BLIND, because the leading anchor only matches a comment that begins its own line. THE SAFE FORM, and it is what the tree already uses at snapshot-grading.structure.test.ts:14-20 and useSnapshotGrade.wiring.test.ts:44-51: split on a CR-tolerant line-feed pattern, then strip an UNANCHORED line-comment pattern per line. Safe on both modes - the split consumes the carriage return, and the unanchored pattern catches a comment anywhere on the line. WHY MODE 2 MATTERS AS MUCH AS MODE 1: useSnapshotGrade.wiring.test.ts:38-43 records an EXECUTED DEFEAT that forced the in-tree form - replacing live code with a void statement plus the same code as a TRAILING comment left every assertion GREEN under a line-start-only strip. That is the L9 defect exactly, and the anchored form does not stop it. CONSEQUENCE FOR THIS ROW''S OWN FIX: converting 53 copies to the anchored form would close mode 1 and OPEN mode 2 across the whole repo. Convert toward the in-tree form instead, and keep the CR canary that useSnapshotGrade.wiring.test.ts:58-70 builds with String.fromCharCode(13). METHOD NOTE, the second this row has earned: the first filing measured SYNTAX rather than BEHAVIOUR and was wrong by a factor of 53; this correction is about probing ONE failure mode and generalising to "safe". A probe that only tries the input you already suspect confirms whatever you already believe - vary the input along every axis the helper claims to cover. |
| The loop's own docs, gates and process debt | L14 | unscoped | A multi-path `vitest run` SILENTLY DROPS a path that matches nothing and still exits 0, so every verify command built as a list of files reports green while covering less than it claims. MEASURED 2026-09-20 twice, independently: `npx vitest run src/lib/no-emojis.test.ts src/does-not-exist.test.ts` prints "Test Files  1 passed (1)" / "Tests  18 passed (18)" and EXIT=0. Trailing arguments are FILTERS, not paths, and a filter matching nothing is not an error. Compounding it, the backlog''s own closure-runner cannot catch the shortfall: src/tools/backlog/closure-runner.ts:31 TESTS_SUMMARY_RE matches the "Tests" line and never the "Test Files" line, and it requires only that at least one assertion passed - so a verify whose paths are half typos closes its row green. | - | - | - | Found by the A10 scoping revision and re-run independently by the orchestrator from PowerShell, same output and same exit code both times. Scope of the exposure TODAY: none live - `grep -c "^  verify: " docs/backlog.yml` is 30 and every one of them is null, so no open row rests on a multi-path command. The exposure is FORWARD: the A8 re-scope already proposes a THIRTEEN-path verify, and a single-path verify IS runner-decidable because a missing or renamed sole path exits 1 with "No test files found" (covered by an executing test at closure-runner.test.ts:34-41). Also worth recording alongside: runVerify has ZERO production callers (grep -rn "runVerify" src returns 8 lines, 4 in closure-runner.ts and 4 in its own test; its doc comment at :71-76 says so), so a verify string is documentation of a decidable check rather than an executing gate until something calls it. | A10 scoping revision, 2026-09-20; re-measured independently the same day | THE FIX IS A CHOICE and should be decided before the next verify is written, because two open items (A8, A10) are about to need one. (a) RULE that a verify is always SINGLE-PATH - simplest, runner-decidable, and it forces the oracle for a chunk into one file, which is arguably the right pressure anyway; the cost is that a chunk whose guarantees genuinely span several landed test files cannot name them all, and must lean on the wave gate instead. (b) TEACH THE RUNNER to read the "Test Files N passed (N)" line and compare N against the number of paths in the command - mechanical, but it has to parse the command. (c) REQUIRE `--reporter=json` and assert the collected file set - most precise, most work. RELATED: this is the same family as L13 - an instrument that reads clean while measuring nothing - and the same family as the repo''s standing rule that a measurement names its command, extended to say the command must be shown to MEASURE WHAT IT CLAIMS. Whoever takes this should also settle whether runVerify ever gets a caller, since a decidable verify nothing runs is still not a gate. |
| The loop's own docs, gates and process debt | L3 | owner | docs/REGRESSION.md entry "Dead tokens" (heading at :37641) says "None were ever defined" of its token list at :37643-37645, and that is not true of every entry. MEASURED 2026-09-14: --field-border IS defined in BOTH themes, at src/app/globals.css:25 (#cbd5e1) and :294 (#334155). Others in the same list are genuinely absent - grep -c "^\s*--error:" src/app/globals.css returns 0, against a canary of "^\s*--danger:" which hits. So it is ONE anomalous entry in a list of thirteen, not a collapsed claim. | - | - | - | THE NUANCE THAT MAKES THIS AN OWNER CALL RATHER THAN A FIX, and it is why I did not simply correct it: the list entry reads "`--field-border` fallbacks" - phrasing that differs from every other entry in the list, which are bare token names. So the entry may be about a FALLBACK USAGE (a var(--field-border, #hex) call site whose fallback never adapted to dark mode) rather than about the token being undefined, in which case "None were ever defined" is loose prose around a correct observation rather than a false claim. Only the person who wrote the entry knows which was meant. FOUND BY the L2 confirmation check while grounding the new visual/aesthetic checker question in this entry; the same pass found and I corrected a separate, unambiguous defect in the same heading - it said "seven of them" above a list of thirteen, fixed in this chunk's commit. | L2 confirmation check, 2026-09-14 | THE DECISION NEEDED, one line: did "None were ever defined" mean the TOKENS were undefined (in which case --field-border is a mistake in the list and should be struck or annotated), or did it mean the FALLBACK EXPRESSIONS never adapted to dark mode (in which case the prose wants tightening but the entry is sound)? COST OF LEAVING IT: low but real and compounding - REGRESSION.md is the durable behaviour record, nothing in the suite reads docs/, and this repo has already shipped one remembered-not-measured quantity in a loop document (891 against a real 900). An entry that is wrong in one of thirteen particulars is exactly the kind a future agent cites wholesale. NO AGENT SHOULD GUESS THIS - the two readings are both plausible and the evidence cannot distinguish them. |

## Features

| area | id | state | title | owns | verify | blocked_by | instrument | from | note |
|---|---|---|---|---|---|---|---|---|---|
| Grading runs: bounds, failures, not-attempted rows, class-trends signal | N13b | unscoped | Make the class-trends draft worth sending: count and name a SUBSET of students who missed the same area, not just an all-or-nothing whole-class verdict. Owner, 2026-09-15. Split out of N13 because it is a feature, while N13a is a bounding change. | - | - | - | Direct read of src/lib/grade/class-trends.ts:124-196, src/lib/gemini.ts:25,124-125 and src/lib/grade/engine.ts:126, 2026-09-15 | N13, owner decision 2, 2026-09-15 | OWNER, VERBATIM: "make the draft more worthwhile. even in a class of 9 where 4 students miss the same area of the homework in a way that doesn't meet the current criteria for drafting an announcement, it's still worth sending out an announcement or contacting those 4 students individually". WHY THE CURRENT DESIGN CANNOT SAY THAT, measured: classifyDirection (src/lib/grade/class-trends.ts:178-196) reads "consistently" literally - EVERY percent value in an area must be >= HIGH_PERCENT_THRESHOLD or <= LOW_PERCENT_THRESHOLD - so 4 of 9 is neither high nor low and renders no clause at all. On any realistically mixed class the modal output is the opening sentence and nothing else, which is what made the original N13 decision 2 a question about whether the feature was worth sending at all. The owner's answer is that it is, once it can see a subset. THREE PARTS. (a) A SUBSET/CLUSTER classification beside the existing direction, with a DERIVED rather than arbitrary threshold - and an explicit answer for small classes, where a fraction is loud but a count is not (3 of 4? 2 of 3?). (b) PER-STUDENT ATTRIBUTION, which layer A does not have: AreaTrend (class-trends.ts:124-150) carries percentValues as an ANONYMOUS number[], so even once a subset is counted the feature cannot NAME the students the owner wants to contact. Identity exists upstream (GradeResult carries student, a Canvas run carries userId); the design must say exactly where attribution enters. (c) TWO OUTPUTS, because the owner named two actions - an announcement to the class and a per-student list. A CLASS ANNOUNCEMENT NAMING STUDENTS IS A PRIVACY DEFECT: the announcement must describe the area without naming anyone while the per-student list stays instructor-facing, and that distinction needs an oracle that FAILS if a student name can reach the class-addressed text. HARD CONSTRAINT INHERITED FROM N11: prose generation stays WITHDRAWN. REGRESSION 423 records that layer C deliberately does not call a model, because every claim it makes was already typed by layers A and B, and reintroducing generation reopens the provenance problem the two-renderer construction closes by type. The fix is MORE COUNTED SIGNAL IN LAYER A. Anyone who concludes generation is unavoidable must argue it explicitly, never smuggle it in. Both outputs are surfaces, so per this repo's own rule the surface is a layer - do not ship a library with no way to reach it. ClassTrendsPanel.tsx and classTrends.wiring.test.ts are the existing surface. OWNER SET THE THRESHOLD 2026-09-15, so it is no longer something to derive. Verbatim: "honestly, i want any trend where three or more students missed points on the same sections called out". THREE OR MORE STUDENTS, AN ABSOLUTE COUNT, not a fraction of the class - so 3 of 4 and 3 of 30 both qualify, and the small-class question this row used to pose is answered by not asking it. AND THE PART THAT CHANGES MORE THAN THE NUMBER: "MISSED POINTS" IS ANY DEDUCTION, not the existing low-score rule. The shipped design only ever notices a student at or below LOW_PERCENT_THRESHOLD (60), so a student who scored 8/10 on an area is invisible to it today. Under the owner's rule that student HAS missed points and counts toward the three. That is a much broader signal than classifyDirection's and it is the real reason the current feature renders nothing useful: it was looking for failure, and the owner wants to see FRICTION. The scoping must therefore establish, and not default: what counts as a deduction when a score is unparseable or carries no scale (the module already refuses to invent a denominator - requirement 4, class-trends.ts:182-187 - and that refusal must not silently become a not-missed), and whether a trivial deduction (19.5/20) should count, since under a literal reading it does. Recommend, do not decide silently. The existing high/low direction is NOT replaced by this - it is a separate, stricter signal that can stay; the subset call is additive. ORCHESTRATOR RULING 2026-09-15, settling the one design decision the check (round 1, NOT READY, 3 blockers) said could not be relocated to an implementer because the implementer's safe-looking default is the SILENT one. THE GATE: class-trends-draft.ts:179-182 filters EVERY clause through areaFullyCovered, which requires area.resultsWithArea === report.totalResults. The design never said whether the new subset clause passes that filter, and both branches are defective as written. RULING: THE SUBSET CLAUSE IS NOT GATED ON FULL COVERAGE. The owner's rule is an absolute count - three or more students missed points on the same section - and that fact is TRUE whether or not every other submission was gradeable. Suppressing it because one submission failed to grade is precisely the silence the owner asked us to fix. THE DISCLOSURE THAT MAKES IT HONEST, and it is not optional, because the existing gate exists for a real reason (the header at :86-93 warns that a clause backed by 5 of 30 with nothing disclosing the gap is the hazard): the subset clause must state its OWN denominator - "4 of the 8 submissions that included this section" - rather than inheriting the opening line's totalResults. Tie every rendered clause's basis to the number it was actually computed from. A LIVE DEFECT THE CHECK FOUND ON THE WAY, which is a second and independent cause of the owner's "renders nothing" report and is NOT fixed by the subset signal alone: because the gate compares against totalResults, ONE FAILED SUBMISSION IN A RUN OF FIVE SWITCHES THE WHOLE DRAFT OFF, today, at shipped defaults. Executed, not reasoned: 5 graded with 4 low on Testing plus 1 grading-failure row gives coverage 4/5 and status=empty, while the same five with no failure row gives status=ok. N13a makes this ROUTINE rather than exceptional, because a deadline-stopped run produces failure rows by design. The same ruling covers it - clauses disclose their own basis instead of being suppressed - and the fix lands in class-trends-draft.ts, which is already in both owns lists. |
| One upload per student: boundary, auto-grade, rubric ingest | N15-rubric-picture | unscoped | Snap / paste / upload a PICTURE of the rubric and of the assignment description, on the assignment-upload grading tool. The assignment-upload tool still has no image-rubric path (confirmed: RubricInputModal.tsx accepts only .docx/.pdf/.txt/.md, and extractTextFromFile has no vision fallback) - but the screen-capture-to-rubric-transcription mechanism this row called hypothetical now exists and shipped (7c1c63f, N14), giving option 3 a concrete implementation to reuse rather than invent. | - | - | - | src/app/components/grading-recording/RubricInputModal.tsx:127 (ACCEPTED_EXTENSIONS, no image type - corrected path, the row omitted the grading-recording/ directory), src/lib/syllabus-upload-validation.ts:20 (ALLOWED_EXTENSIONS agrees), src/app/actions/syllabus-upload.ts:39-70 (extractTextFromFile, zero LLM calls - corrected path, the row omitted the app/actions/ directory), all re-confirmed 2026-09-15. NEW: git show 7c1c63f --stat and a direct read confirm src/app/actions/snapshot-transcribe-rubric.ts, src/app/components/snapshot-grading/snapshot-rubric-capture-prompt.ts, useSnapshotRubricCapture.ts, and SnapshotRubricCaptureReview.tsx all exist and are wired (useSnapshotRubricCapture.ts:21,122 imports and calls snapshotTranscribeRubricAction). | owner request, 2026-09-15 | THE LESSON STANDS (brief from the tree, not memory), and the file-path corrections above are examples of the same discipline. WHAT CHANGES: this row's "three quite different builds" framing for the picture-in-assignment-upload-tool question should no longer treat "lift screen capture out of snapshot-grading" as pure speculation - N14 (7c1c63f) already built exactly that capture-to-transcription pipeline for snapshot-grading, so a scoping pass should read useSnapshotRubricCapture.ts and snapshot-transcribe-rubric.ts as the concrete precedent to extract/share, not as one of three equally-unbuilt options. The underlying product question (paste vs. file-picker vs. shared screen-capture) is unchanged and still not to be defaulted. |
| One upload per student: boundary, auto-grade, rubric ingest | N15a | unscoped | The owner's one-upload-per-student workflow on the assignment-upload tool: tick a checkbox meaning each upload is one student, then upload repeatedly, each upload recognized as a different student and graded as it lands. FORMERLY TWO ROWS (N15a dispatch, N15b queue) - MERGED by Ruling N15-3 after the scope check showed the seam was cut through the middle of one correctness property: per-upload dispatch WITHOUT the queue is not a smaller working feature, it is an unserialized dispatcher, because getGeminiInterRequestDelayMs is applied only BETWEEN entries inside one gradeStudentEntries call (engine.ts:207-209), so N one-entry calls pay it zero times. What survives from the scoping pass: gradeEntries (engine.ts:333-341) really is a generic per-entry wrapper, and canvasWorkToEntry (extraction.ts:153) is a real precedent for building one entry directly. | - | - | - | Scope check of scratchpad/n15-scope.md, 2026-09-15: DEFECTIVE, 8 blockers, 4 majors. Verdict: may not be built as scoped. | owner request, 2026-09-15 | RETURNED TO UNSCOPED DELIBERATELY. The owns list it carried was computed from the description rather than from the code, and the check named eleven files missing from it - including src/app/actions.ts (the barrel GradingTab imports through, without which the action ships dead), src/lib/grade/types.ts (GradeResult has NO id field, so the stable-row-id fix cannot be made without it), src/lib/grade/engine.test.ts (five describe blocks on the very function both chunks call), and action-guard-coverage.test.ts (a new server action fails it unless requireOwner() is textually first in its body). Keeping a wrong owns list would be worse than having none. THREE FINDINGS AN ARCHITECT PASS MUST CARRY, each measured: (1) GradingTab.tsx:150-155 smooth-scrolls to the results section on every run change, so in an incremental flow it drags the viewport away from the file input ONCE PER STUDENT - X-1's named defect, sitting in an owned file, uncounted. (2) The results table sorts by student name unconditionally (DEFAULT_SORT, gradingResultsHelpers.ts:99-102, pinned by a landed source-reading test), so incremental rows insert ALPHABETICALLY, not at the end - the just-graded row lands anywhere in a class of 30. (3) gradeAction's Gemini path is a Promise.all of THREE calls plus a fourth when the rubric box is blank; gradeStudentEntries returns fullCreditChecklist: [] hardcoded (engine.ts:276), so per-upload grading silently ships a degraded results panel with every gate green - and if the rubric box is blank, each upload generates its OWN rubric, so students are graded against different rubrics. Also unsized: the owner's primary input is a phone photo, UPLOAD_WIRE_BUDGET_BYTES is 3.5MB (upload-budget.ts:41) and base64 inflates by 4/3, so a real photo fails on upload one. OWNER DECISION 2026-09-15, recorded here because it arrived while scoping a different item and it CONFIRMS this row's premise from the other direction. Verbatim: "each student will have their own zip". It was given as the answer to a re-run identity question on bound-stopped grading runs - the zip path has no durable per-student id, so a stopped run could not name what to re-run. One upload per student dissolves that: the upload is the identity. TWO CONSEQUENCES FOR THIS ITEM. (1) The per-upload-is-one-student mode is not only a convenience for the owner's grading workflow, it is what gives every downstream feature a re-run key on the zip path - so the checkbox this row describes carries more weight than its size suggests, and A12's not-attempted rows depend on it for that path. (2) THE QUESTION THIS ROW MUST STILL ANSWER: whether the multi-student bulk zip stays supported alongside it. If it does, two identity regimes coexist and every consumer of a stopped or failed row has to know which it is looking at; if it does not, that is a removal with its own blast radius. Do not default it. |
| Repo grader workflow and state legibility | A7 | unscoped | Overhaul the repo grader workflow. Owner, 2026-09-15: "the workflow on the repo grader page could use an overhaul to be more streamlined, user friendly, intuitive, and telegraph what state things are in". THE FIRST THING ANY PASS MUST KNOW, and it is why this is filed unscoped rather than dispatched: A UX OVERHAUL OF THIS EXACT VIEW ALREADY SHIPPED. docs/repo-grades-ux-overhaul-acceptance-criteria.md is 2172+ lines and REGRESSION.md:33460 records the delivery, noting that every one of its four pre-code artifacts was rejected by an adversarial pass at least once. So this request is a SECOND overhaul of a surface that has already had one, and the honest first question is what the first one did not fix, or what it fixed and then regressed - not a fresh re-derivation. | - | - | - | @(Get-Content <path>).Count over src/app/components/repo-grades/*.tsx, re-measured 2026-09-15, plus src/file-size-ceiling.structure.test.ts:30 (LIMIT = 1000). | owner request, 2026-09-15 | MEASURED SIZES, RE-MEASURED 2026-09-15 (each grew since the row was filed, most from concurrent A5/A6 work in the same directory the row itself flags as a blocker): index.tsx 913 (was 912), RepoGradeCellControl.tsx 651 (was 628), RepoGradesControls.tsx 647 (was 634), RepoGradesGrid.tsx 605 -> 643, LinkUsernamesPanel.tsx unchanged at 453. index.tsx is now 87 lines from the 1000-line ceiling (src/file-size-ceiling.structure.test.ts:30), not 88 - re-measure again before scoping, since A5/A6 may still be landing. So an overhaul CANNOT be additive - it must extract before it adds, and the extraction is the wave, not a follow-up ticket. BLOCKED ON A5 AND A6 BY FILE SET, not by logic: both are in flight against RepoGradesControls.tsx and RepoGradesGrid.tsx right now, and parallel-disjointness.md forbids a third item in the same files. They are small and land first. WHAT THE SCOPING PASS OWES, and the order matters: (1) read docs/repo-grades-ux-overhaul-acceptance-criteria.md FIRST and list what it already decided, so this pass does not re-litigate settled questions or, worse, silently reverse them - it records specific decisions about sticky headers, badge systems, focus styles, the score box, and the mobile reflow; (2) the owner named FOUR things - streamlined, user friendly, intuitive, and TELEGRAPH WHAT STATE THINGS ARE IN - and the fourth is the most concrete and the most testable, so inventory every state this view can be in today (scanning, scan error, per-repo folder-scan failure, ungraded, grading, graded, missing folder, posting, posted, post failed, rate-limited, binding unconfirmed) and say for each where it is shown, how, and whether two states share one appearance; (3) the same doc records that this view runs TWO BADGE SYSTEMS in one surface and calls that a defect, which is exactly a telegraph-the-state problem the owner is now asking about independently - start there. RULING X-1 APPLIES AND IS THE ACCEPTANCE NUMBER: count interactions per graded repo before and after, setup separately from steady state. |
| Ask AI modal: persistence and chip copy | A3 | unscoped | Persist AskAiModal answers so a later session reads them back. The ideation pass ranked this FIRST; the check demoted it, because as scoped it is not the corpus class - it asserted the WRITE and left the read-back unguarded, and docs/loop/leverage.md:61 strikes generic persistence except where a record is "genuinely read back later, not for merely writing a row". Delete the read-back and the proposed test stays green. | - | - | - | L7 ideation check, 2026-09-15: four required changes, two of which contradict the proposal's own "no schema change" headline | owner request, 2026-09-15 | UNSCOPED because two of the four changes are design decisions rather than edits. (1) THE REUSE CONTAMINATES A SHIPPED FEATURE, and this is the serious one: course-intel.ts:113 calls listAllCourseIntelAnswers(supabase, user.id) with NO course filter and NO feature filter, and :132 calls clearAllCourseIntelAnswers on the same scope. So writing AskAiModal answers into course_intel_answers makes them appear in the Course Intel tab history list and puts them under that tab Clear history button - every gate green, another feature user-facing list quietly filling with unrelated rows. The honest fix is a source discriminator column or a separate table, which is the schema change the proposal said it avoided. (2) THE ROW SHAPE DOES NOT FIT: AppendCourseIntelAnswerCommon (history.ts:415-431) requires citedStudents, omissions, assembledAt and tier: AssemblyTier, and AssemblyTier is signals or signals+text (types.ts:385) - an AskAiModal answer is neither, so the build either writes a false tier or widens a union across three files. (3) The read-back must move into a server action to be assertable at all, since nothing renders here. (4) Persisting forces a userId and therefore an auth guard onto askAboutCourseAction, which is PINNED as deliberately unguarded at action-guard-coverage.test.ts:248 in a list of 28 - a correction to my own "about twenty", measured by awk over the block plus grep -c. That test is exact-set equality, so the pin comes out in the same commit; the check rules this the CHEAP part. Also unflagged by the ideation pass: llm-content.ts imports no Supabase at all today, so this puts a service-role DB dependency on the path of a user-facing modal that currently needs none, and an insert that throws must not cost the instructor the answer they just waited for. |

## Chores

| area | id | state | title | owns | verify | blocked_by | instrument | from | note |
|---|---|---|---|---|---|---|---|---|---|
| Grading runs: bounds, failures, not-attempted rows, class-trends signal | N13a | unscoped | The grading CAP became an honest bound of 40 plus a wall-clock deadline (shipped 7c409a6) - still owed: remove the class-trends-draft FLOOR (class-trends-draft.ts:18, enforced at :154) and correct its stale :86-88 comment, which still claims the floor and cap are both 5 and cites the constant's old gemini.ts:25 location. | - | - | - | git show 7c409a6 --stat, 2026-09-15, confirms the cap/deadline half shipped (engine.ts deadlineMs at :187/:207-210, GradeResult -> GradedResult \| UngradedResult union in types.ts). Direct read, 2026-09-15: gemini.ts:32 (DEFAULT_MAX_SUBMISSIONS = 40), gemini.ts:129-133 (getGeminiMaxSubmissions), class-trends-draft.ts:18 (DEFAULT_CLASS_TRENDS_DRAFT_FLOOR = 5, unremoved), :86-88 (stale comment), :154 (floor still enforced), class-trends.ts:124-196 (AreaTrend/classifyDirection, unchanged and still accurate). | N13, owner decision 1, 2026-09-15 | THE FLOOR is NEXT_PUBLIC_CLASS_TRENDS_DRAFT_FLOOR - layer C refuses to draft below 5 graded submissions. Straight removal, no replacement. RECORD THAT IT WAS DELIBERATE: the floor existed because the draft is addressed to STUDENTS rather than to the instructor who did the grading, so a message written off two submissions goes out to a whole class. The recommendation on file was keep-the-floor-raise-the-cap; the owner overrode it knowingly and that is the owner's call to make. THE CAP is DEFAULT_MAX_SUBMISSIONS (src/lib/gemini.ts:25), read via GRADE_MAX_SUBMISSIONS (:124-125) and applied by .slice(0, maxSubmissions) at src/lib/grade/engine.ts:126. DO NOT SIMPLY DELETE IT, and this is the whole reason this row exists separately: the cap is not only a cost knob, it is also what keeps a grading run inside the platform function limit. This deploys to Vercel Hobby, where maxDuration is capped at 60s. Delete the constant with no replacement bound and a large class times out MID-RUN - strictly worse for the instructor than a capped run that completes, because a timeout loses the work already done. The scoping must measure the real constraint (the route's maxDuration, the per-submission latency assumptions, any existing batching) and propose what replaces it: a much higher explicit ceiling, batching, a resumable run, or something better. Obeying the instruction literally here would ship the failure, so the deliverable is the bound, not the deletion. OWNER SET THE BOUND 2026-09-15, so it is no longer a question to escalate. Verbatim, in two messages: "we need to raise that to at least 20", then "raise it to 40 - we'll rarely go that high". THE CAP BECOMES 40, and the second half of that sentence is the part that changes the design rather than just the number: 40 is intended as a RARELY-TOUCHED SANITY CEILING, not as the working limit. THE CONSEQUENCE THE OWNER HAS ACCEPTED, stated so nobody re-litigates it: the count bound is no longer what keeps a run inside the platform invocation ceiling. That protection has been deliberately traded away, so src/lib/visualizer/selection-coverage.ts:62-100's written rule - that the count bound IS the actual defense against a platform kill - cannot simply be ported to this loop. Whatever ships must either argue why the grading loop differs or supply a different survival mechanism. THE ARITHMETIC THAT REFRAMES THE ITEM, modelled and NOT measured, flagged as such because the real per-call latency is not measurable in this checkout: engine.ts:207-209 applies getGeminiInterRequestDelayMs between entries (DEFAULT_INTER_REQUEST_DELAY_MS = 1200, gemini.ts:60) and this repo's own written model in selection-coverage.ts puts an LLM call at roughly 5s, so a student costs on the order of 6.2s and a run of TEN is already near a 60s grant. If that holds, the cap of 5 has been MASKING a pre-existing timeout, and raising it does not create the problem so much as expose it - which also means the owner's own working case, a class of 9 to 12, may already be at the ceiling today. That reframing, not the constant, is the item. SO THE DELIVERABLE IS NOW SURVIVAL, not sizing: incremental persistence so finished students are durable before an invocation dies, a Route Handler that declares its own ceiling, or chunked/resumable execution across invocations - with the not-attempted disclosure as the REPORTING half rather than the protection half. IMMEDIATE LEVER, owner-only and requiring no deploy: GRADE_MAX_SUBMISSIONS is read at runtime from the environment (gemini.ts:122-127), so setting it to 40 in the Vercel dashboard raises the bound in production now; DEFAULT_MAX_SUBMISSIONS = 5 in code is only the fallback, and parsePositiveInt silently falls back to it on a malformed or non-positive value, so a typo in the dashboard reverts to five without complaint. Raising the constant in code is still owed, because the fallback should not contradict the owner's decision. NOTE THE COLLISION this creates, already documented at class-trends-draft.ts:87-88: the class-trends draft floor and the cap are both 5 today and that file's comments reason about them being equal. At 40 they are not, and those comments go stale in the same change that deletes the floor. DISPOSAL ROUND 2026-09-15. The round-2 artifact came back NOT READY (4 blockers, 7 majors), and under iteration-caps.md this artifact is now AT ITS DISPOSAL ROUND on two independent grounds: cap 2, because round 2 was its second revision, and cap 1, because owns-list-caller-misclassification has now failed TWICE and the second attempt strengthened the same mechanism - a longer, better-cited owns list - instead of changing kind. No further revision of the owns list by a design seat is legal. THE FOUR DISPOSALS. (a) RELOCATE, the owns list: it moves to a step that DERIVES it with its own instrument rather than authoring it again. RECEIVER: the implementer wave that opens the build. OBLIGATION: run the enumeration itself over gradeAction, getGeminiMaxSubmissions, composeClassTrendsDraft and below-floor, treat every list in these artifacts as a FLOOR and never as the set, and open every call site rather than describing it. The instrument is grep plus opening the file; the step is the wave gate, which already diffs git status --short against the assignment. WHAT THE SECOND FAILURE ACTUALLY MISSED, recorded so the derivation starts ahead rather than level: GradingTab.tsx is NOT a caller of the grading path (it imports only fetchCanvasMetaAction and a type), while the real callers are src/app/page.tsx:63 via useActionState, and three workflow registry steps - steps.grading-run.ts:473,533, steps.grading-draft-flow.ts:262 and steps.grading-cartridge.ts:100 - none of which appeared in either round. src/lib/grade/engine.test.ts and src/lib/grade/class-trends.test.ts are also owed. (b) REDUCE, two questions to the owner, batched and not gating. (c) RESIDUAL, the owner-blocked measurements, escalated rather than parked with a future implementer: the REAL platform grant for the current Server-Action path, and ONE timed grading run against a live key. Until both exist every per-student latency figure in these artifacts is modelled, the chunk size is a starting point and must be labelled one, and at this repo's own 25 percent margin standard that starting point is SEVEN, not eight. (d) DELETE: nothing. ORCHESTRATOR RULINGS, mine, because the check's stopping point was rulings and re-dispatching the author on them is forbidden. RULING 1, SCOPE: N13a COVERS THE UNATTENDED CALLERS, and this is the finding that would have shipped a silent regression. getGeminiMaxSubmissions is read ONCE, at engine.ts:122, on the shared path - so raising the default to 40 raises it for EVERY caller, including three workflow registry steps that run server-side inside a cron tick whose route declares maxDuration = 60 (src/app/api/cron/run-schedules/route.ts:56). Built as round 2 specified - client-driven chunking - the browser path would be safe and the unattended path would attempt up to 40 students inside a 60s tick and lose the entire run, with lint, tsc, next build and all 94 tests green, because nothing here renders a component or runs a tick. A mechanism that only exists in the browser cannot bound a knob that is read on the server. RULING 2, REINSTATE THE WALL-CLOCK DEADLINE. Round 2 withdrew it on the ground that the owner's decision "superseded" it; that is a non-sequitur - the owner settled a COUNT, which says nothing about whether the loop should refuse to START entry N+1 past a deadline. The withdrawal removed the only proposed mechanism that works inside a single server-side invocation, which is exactly what ruling 1 requires. It is also already shipped in this tree and was never cited: cron/run-schedules/route.ts:123-126 computes runDeadlineMs = now + 50_000 and stops starting new institutions past it so the rest resume next tick, threaded as deadlineMs at :372 and :515. That is the precedent to port. RULING 3, THE NOT-ATTEMPTED ROW IS A SEAM, not a detail, and it goes to the TOP TIER rather than to a design seat: A12 and N13b are both blocked on its exact shape, three downstream consumers read it (class-trends.ts, gradeReposAction's reporting arrays, grading.ts's Canvas post), and round 2 both demoted it to "the reporting half" and left it unspecified while asserting elsewhere that it folds into unscoredCount. Those two cannot both hold. THE CONSTRAINT THAT SHAPE MUST SATISFY, discovered by the check and load-bearing: a not-attempted row must be appended to results BEFORE engine.ts:226's canonical-column reconciliation. Appended after, it reproduces the exact 4-of-5 coverage defect recorded against N13b - N13a's own feature resurrecting the defect this pass spent a page retiring. AND IT MUST NOT BE POSTABLE TO CANVAS: see A13, filed separately, where a FAILURE row already reaches the student today. ONE RECORDED DEFECT IS RETIRED, with a caveat. The check settled by reading what round 2 could only call likely: engine.ts:226-247 gives every result an entry for every canonical area, blank-filling at :245, and class-trends.ts:274 counts every entry present, so a failure row leaves resultsWithArea equal to totalResults and the clause DOES render. The "executed, not reasoned" 4-of-5 example in N13b's note does NOT reproduce against the real engine and was almost certainly executed on a hand-built fixture whose failure row never passed through reconciliation - this repo's own recorded failure mode. N13b's ruling that clauses disclose their own denominator still stands on its own merits; only the worked example is withdrawn. The confirming test, when written, must build the row THROUGH gradeStudentEntries, or it will re-confirm the fixture instead of the code. ALSO CORRECTED: round 2 misquoted its own margin anchor - selection-coverage.ts:68 says roughly 15s and 25 percent, not 13s and 22 - and labelled the Server-Action grant "confirmed" when selection-coverage.ts:51-58 says in terms that it was never independently verified. SEAM DISPOSAL AND SIX RULINGS 2026-09-15. The not-attempted row seam (scratchpad/not-attempted-seam.md) reached its disposal round: round 2 was its second revision, and its round-2 check returned 6 blockers over FOUR classes, EVERY ONE A REPEAT. Under iteration-caps.md cap 1 a repeat goes to disposal immediately and a third attempt would be strengthening the same mechanism, which is forbidden; under cap 2 round 3 produces only dispositions. THE AUTHOR IS NOT RE-DISPATCHED. The check also established that with these six rulings applied THE SEAM IS SHIPPABLE: its type shape compiled clean over ten requirements the author never probed, its door table was independently re-derived and matches at all 8 call sites, its consumer enumeration was accurate at every line opened, and its reconciliation against the landed checkRowPostability (6 of 8 doors covered, the 2 CSV doors left as the seam's own residual) is correct. RULING 1, THE FAILURE MESSAGE: ungraded.message CARRIES the "This submission could not be graded: " prefix. The artifact defined it two incompatible ways - section 2's field doc said the verbatim thrown message, section 5 said the prefix is its opening - and an implementer pasting section 2's code block would have picked the one that silently changes today's string and falsifies the artifact's own byte-identity claim. Carrying the prefix preserves engine.test.ts:108 and today's displayed text. CONSEQUENCE THAT MUST SHIP WITH IT, closing the artifact's major 6: GRADING_FAILURE_PREFIX currently lives at src/app/components/grading-recording/grading-rows.ts:214, and src/lib/grade/engine.ts MUST NOT import from a component directory - that is the layer inversion the seam itself forbids three paragraphs earlier. MOVE the constant into src/lib/grade/ and have grading-rows.ts import it from there. Do not duplicate the literal; two copies of a user-visible prefix is exactly the drift this repo keeps re-discovering. RULING 2, THE CSV DOORS: the refusal at the gradebook-CSV doors is isUngraded(row) AND checkRowPostability(...).postable === false - NOT isUngraded alone. The artifact mandated an unconditional isUngraded refusal, which contradicts the shipped A13 guard and the owner's own decision: steps.grading-singles.ts:592 admits a row only when a reviewer typed a real numeric grade, which is precisely the RESCUED row that A13 clause (c) deliberately lets through. Unconditional refusal would post a rescued row to Canvas while silently vanishing the same row from the gradebook CSV built from the same approved set - a divergence no gate here can see. RULING 3, THE REVIEW INSTRUCTION IS BY PROPERTY, NOT BY SPELLING: NO CONSTRUCTION MAY PRODUCE AN OBJECT CARRYING BOTH userId AND ungraded. The artifact told reviewers to look for `as GradeResult`, and the check compiled two CHEAPER escapes that carry no cast and no assertion for a grep to find - Object.assign onto a typed row, and assignment through a loosely-typed alias - both of which are the natural shape in parseGradeResult and coerceGradeResult, which build field by field from untrusted input. A spelling-based review instruction cannot catch them. The sanctioned repairs are a branch or a conditional spread. RULING 4: add fillGradebookCsv to the wiring oracle's builder-name seed. The door TABLE is complete, but the durable instrument was seeded with only postCanvasGradesAction, buildCanvasGradebookCsv and buildMoodleGradebookCsv, and fillGradebookCsv (steps.grading-singles.ts:660, writing scores into a real gradebook file at gradebook-csv.ts:463) would let a future module reach a gradebook through a door the oracle cannot see. RULING 5, AND THIS IS THE SILENT-GREEN ONE: the factory carries codeExecution, gradedRepo and gradedRef UNCHANGED, and the oracle asserts all three. The artifact's field table omitted them while its prose claimed byte-identity with today's failure row, so built literally a failed repo grade would lose the repo and commit that types.ts:154-159 exists to record - the evidence for defending a grade to a student - and lose the sandbox run. Nothing here would catch it: the allowlist coverage test uses a hand-built sentinel rather than the engine, engine.test.ts asserts none of the three, and no component renders. RULING 6, THE SENTINEL GUARANTEE: every field on every sentinel EXCEPT that member's own excluded key (a two-element exclusion set) - NOT the artifact's proposed SENTINELS.some(...), which is strictly WEAKER than the check it replaces while being described as stronger. Under .some, a future optional field given a value on the graded sentinel and omitted from the ungraded one passes, and the ungraded sentinel's three round-trips then preserve it trivially - verbatim the defect that check's own comment says it exists to catch. ALSO CARRIED FORWARD, from the check and not disputed: engine.ts:162-171 does NOT compile under the union and the fix is narrowing gradeSubmission to Promise<GradedResult>; "unpostable by construction" is honestly "closes the one hole present today"; two producers were missing from the table (embedded-grader/index.ts:196-211 and discussion.ts:471-486, hidden from a grep requiring "resubmitNotice:"); submissionTruncated has NO production rule on a not-attempted row and cannot be "carried from the entry" because StudentSubmissionEntry has no such field and truncateSubmission never runs for an unattempted one; report.ungraded needs a stated production rule; and the O1/O4/O6 oracles must pin a rubric that actually parses criteria or they pass vacuously. N13b IS SETTLED IN THE SEAM'S FAVOUR: the empty-canonical path is real and reachable, a not-attempted row with an empty rubricAreas raises totalResults without raising resultsWithArea, so the class-trends exclusion is REQUIRED rather than optional - the author was right to decline to agree with the orchestrator's paraphrase, and this is recorded as an instance of a seat correctly refusing a handed-down claim. REMEASURED 2026-09-15 - THE FLOOR HALF OF THIS ROW'S OWN TITLE IS STILL OPEN. class-trends-draft.ts was not touched by 7c409a6 (absent from its file list) - DEFAULT_CLASS_TRENDS_DRAFT_FLOOR = 5 still stands at :18 and is still enforced at :154 exactly as before. The header comment at :86-88 is now wrong on two counts, not one: it claims the floor and GRADE_MAX_SUBMISSIONS "are both 5" (the cap is 40 now) and cites "gemini.ts:25" as the cap's location (the constant is at gemini.ts:32; :25 is now an unrelated comment). Whoever removes the floor must also fix or delete this comment rather than leave a citation that points at the wrong constant. The row's instrument line numbers for gemini.ts and engine.ts are themselves now stale post-implementation and are corrected above - re-cite them rather than reusing the pre-7c409a6 numbers. The full ruling history already on file above (the six seam rulings, the owner quotes setting the bound at 40, the sequencing rulings) is KEPT as an accurate record of what was decided and built, confirmed against 7c409a6. |
| requireOwner() call-site authorization | R2 | unscoped | Reclassify every requireOwner() call site into requireUser() or requireAppOwner(). requireOwner is `return requireUser()` (src/lib/supabase/auth.ts:451-452) - any active account passes - across 411 invocations (Get-ChildItem -Recurse src \| Select-String -Pattern "await requireOwner()", re-measured 2026-09-15; equivalent to grep -roE "await requireOwner\(\)" src/ \| wc -l). Its @deprecated block calls this "DELIBERATELY LESS RESTRICTIVE ... a tracked, temporary state". | - | - | - | Get-ChildItem -Path src -Recurse -Include *.ts,*.tsx \| Select-String -Pattern "await requireOwner()" = 411 total matches (PowerShell equivalent of the row's own grep -roE "await requireOwner\(\)" src/ \| wc -l), re-measured 2026-09-15 against the current tree - the row's 446 is stale. requireAppOwner() also drifted: 19 files carry it (was cited as 15) and 23 direct await requireAppOwner() calls exist (was cited as 9). | docs/multi-user-login-acceptance-criteria.md A6 | RE-CORRECTED 2026-09-15: 446 was already flagged once as a copy-paste error from docs/multi-user-login-acceptance-criteria.md; the number has ALSO moved since that correction was written (more requireOwner() call sites were added), so it needs re-measuring at scoping time rather than trusted from either this row or the acceptance-criteria doc. requireAppOwner()'s own counts (15 files/9 calls) are equally stale (now 19/23) - re-measure both before sizing any wave. The rest of the row's substance - the RESOLVED 3 / SUPERSEDES architecture decision, canvas-core.ts and github.repos.ts having zero guard calls today, the media-file cohort as first wave - was not re-verified in this pass and should be re-confirmed before scoping, since it depends on a code state that may also have moved. |
| Orphan-upload sweep | G3 | unscoped | The sweep's per-call latency assumption (~250ms, src/lib/orphan-upload-sweep.ts:46,55) underpins both MAX_USER_PREFIXES_PER_TICK and PHASE1_ROOT_LISTING_BUDGET_MS. A wrong assumption makes the sweep UNDER-clean, which is the safe direction. | - | - | - | - | c25bdec | First tick's timing (pre-migration Note column) |
| Orphan-upload sweep | N3 | unscoped | Flip failedCount to gating in .github/workflows/sweep-orphan-uploads.yml. A one-line edit an agent makes here; blocked on V3's (formerly 1.3's) INPUT, not on capability. Until the semantics are known, a wrong guess in either direction poisons the only outbound alert this deployment has. | - | - | V3 | - | - | Blocked by V3 (formerly 1.3) (pre-migration queue note) |
| Orphan-upload sweep | V3 | verification | Read the orphan sweep's first production tick's raw body. The delete accounting ships OBSERVATIONAL ONLY because what remove() returns for a named delete is unmeasured; matchedByLeafName / matchedByFullPath exist to settle it in one tick. Unblocks N3 (formerly 3.2). | - | - | - | The first run's JSON body in the Actions log | c25bdec | - |
| Orphan-upload sweep | V4 | verification | Confirm the sweep's prefix form matches. A ruling pinned ${userId}/${segment} with no trailing slash; only a live listing proves Supabase agrees. Signature of a wrong guess: a positive scannedUserPrefixes with every other count at zero. | - | - | - | Same tick as V3 (formerly 1.3) | c25bdec | - |
| Orphan-upload sweep | V5 | verification | Confirm the scheduled workflow fires. | - | - | - | The Actions web UI after merge | c25bdec | - |
| The loop's own docs, gates and process debt | L11 | unscoped | Make the owns-list caller check MECHANICAL instead of remembered. L10 shipped the procedure into docs/loop/parallel-disjointness.md, and the implementer who wrote it said plainly in its own report that prose will not be enough - and it is right. The evidence is L10's own history: the correct definition was already in that card, was quoted at people twice, and was still missed five times in one day, the fifth by the orchestrator ONE COMMIT AFTER writing the rule down. A sixth card is not the fix. | - | - | - | L10 implementation report, 2026-09-15, plus the five instances recorded in L10 before it closed (A1, N15, A2, A5 fixtures, A5 index.tsx) | L10 | THE PROPOSAL, which is the implementer's and which I endorse: the WAVE GATE runs the grep the card describes and diffs it against the submitted owns list, rather than a human or an agent remembering to run it. The gate already executes git status --short against the assignment, so the machinery and the moment both exist; this adds a second, cheaper question to the same step. WHY THIS IS UNSCOPED AND NOT JUST DISPATCHED - three things have to be settled first and none of them is obvious. (1) WHAT TO GREP FOR. A change to a function has a symbol; a change to a type, a prop, a CSS class or a string constant has a different one, and some have none an implementer could name. A gate that only works for named exports will be silently useless for the other half of waves. (2) FALSE POSITIVES ARE THE WHOLE RISK. Every caller of a widely-used symbol would be flagged, and a gate that cries wolf on a forty-hit grep gets ignored within a week, which is worse than no gate because it looks like coverage. Something has to distinguish "reached by this change" from "merely mentions the symbol", and that judgement may not be mechanisable - in which case the honest answer is a gate that REPORTS the hits for a human to classify rather than one that passes or fails. (3) WHERE IT LIVES. This repo's gates are vitest files; a wave-gate check is not obviously one, since it is about a brief rather than about the tree. It may be a script the orchestrator runs, which makes it exactly as skippable as the card - and if so, say that out loud rather than shipping a gate that is a card with extra steps. |
| The loop's own docs, gates and process debt | L12 | unscoped | The 1000-line ceiling measures the wrong thing for a canary file, and this repo now has the instance to prove it. repoGrades.wiring.test.ts went red on main (1037 lines) because A5 added 91 lines of new guards, and was split under duress in 25ffe9c. The implementer who split it argued - and I agree - that growth in that file is not rising complexity but a growing COUNT OF INDEPENDENT GUARANTEES: every block declares its own path constant, its own describe and its own assertions, with almost no coupling to its neighbours. So the file will hit the wall again every few features however well it is organised internally, and each hit costs a split done under a red gate rather than by design. | - | - | - | The 25ffe9c split and its implementer's reported analysis, 2026-09-15; the ceiling itself is src/file-size-ceiling.structure.test.ts:30 | N14 wave 2 implementation, which found the red gate from outside the directory | THE PROPOSAL, which is the implementer's: adopt a convention of ONE GUARD FILE PER GUARDED COMPONENT from the start, rather than accreting a single wiring file until it breaches and then cutting it. That is a convention change, not a code change, which is why this is unscoped - it needs a decision before it needs a file list. THREE THINGS TO SETTLE FIRST. (1) IS THE CEILING ITSELF WRONG HERE, or only its application? An exemption list is the obvious move and the obvious trap - an exemption that says "test files may grow forever" removes the pressure that produced two genuinely good splits in this repo's history. Anything proposed must say what still constrains these files. (2) THE SPLIT THAT JUST HAPPENED WAS GOOD, and that argues against the convention as much as for it: the seam it found (click-gating guards versus linking guards) only became visible once both waves existed, and a one-file-per-component rule written in advance would have produced a different, probably worse, cut. So the honest question is whether the convention beats splitting-when-a-real-seam-appears, not whether it beats splitting-under-duress. (3) WHAT ACTUALLY FAILED HERE WAS THE VERIFY LIST, not the ceiling: A5's verify named five commands and the ceiling test was not among them, so nobody ran it - not the implementer, not the verifier, not me. A convention change would not have caught that, and the cheaper fix may be that every verify list in this queue includes the ceiling gate whenever the wave adds lines to an existing file. Consider that before the larger change. |
| The loop's own docs, gates and process debt | L6 | unscoped | Two residuals the L4 build owes, filed together because they share an instrument. (a) LIVE-LOOP is the one derived advantage class with no removal test anywhere in the repo - its example is the snapshot keyboard layer (SnapshotGradingPanel.tsx:516-558), and nothing asserts that the layer stays reachable without a mouse, so the class could erode exactly as the GUARANTEED class's did before L4 caught it. (b) The six classes were derived from the tree on 2026-09-15 and are a snapshot, not a standing truth - the owner's own directive says to re-derive them by clustering shipped features, and nothing schedules that. | - | - | - | L4 verification, 2026-09-15, against docs/loop/leverage.md and a grep for any test asserting on the keyboard layer's reachability | L4, the leverage directive | UNSCOPED because both halves need a decision before a file list means anything. For (a): a removal test for LIVE-LOOP is hard here for the reason the whole repo is hard - no component renders under vitest, so "usable without a mouse" cannot be asserted by rendering. The nearest buildable form is a source-order or handler-presence assertion, which is weaker than the GUARANTEED class's import-prefix guard, and whether that is worth building is a judgement about how much a weak guard is worth. For (b): re-derivation has no natural trigger - it is not per-feature and not per-release, and a cadence nobody honours is worse than no cadence. The honest options are to re-derive when a feature needs a class STRETCHED to fit (the owner's directive already names stretch as the signal that a class is missing) or to not schedule it at all and let the stretch signal fire naturally. The second is cheaper and probably right. |
| The loop's own docs, gates and process debt | L9 | unscoped | Source-scanning tests in this repo can be satisfied by COMMENTED-OUT code, and most of them do not defend against it. Found by the N14 wave 1 verification, on the one assertion least able to afford it: the wiring canary whose entire purpose is to stop the snapshot keyboard layer dying silently. The verifier commented out window.addEventListener while leaving removeEventListener in place and NOTHING caught it - tsc, lint, 311 tests and next build all stayed green, because the canary is a regex over raw source and a regex matches text inside a comment exactly as well as live code. MEASURED 2026-09-15: grep -rln readFileSync --include=*.test.ts src returns 176 files; grep -rln stripComments over the same set returns 57. So on the order of 119 source-scanning test files have no comment defence, though not all of them need one. | - | - | - | Get-ChildItem -Path src -Recurse -Include *.test.ts \| Select-String -Pattern readFileSync -List (176 files) and the same with -Pattern stripComments (57 files), re-measured 2026-09-15 (equivalent to grep -rln readFileSync --include=*.test.ts src / grep -rln stripComments over the same set, which the row's own instrument names). | N14 wave 1 | THE WAVE 1 INSTANCE IS ALREADY FIXED - this row is about the other 119. UNSCOPED because a blanket sweep is the wrong shape and would be worse than nothing: stripComments is NOT always correct. Some assertions deliberately scan comments (this repo pins RULES and CITATIONS in comments, and docs/REGRESSION.md treats a stale comment as a defect, so a test asserting a comment exists is legitimate). Others assert a string is ABSENT, where stripping comments makes the test WEAKER rather than stronger. So the work is a triage, not a sweep, and the triage rule has to be authored before any file is touched. THE RULE I WOULD START FROM, for whoever scopes this: an assertion that something IS PRESENT in code - a call, a registration, a guard - must strip comments, because commented-out code satisfies it while doing nothing. An assertion that something is ABSENT must NOT strip them, because a commented-out forbidden call is still a signal worth failing on. Assertions about comments themselves are a third category and are left alone. WHERE TO LOOK FIRST: the five *.structure.test.ts files, since those are the ones this repo uses as construction guards, and any test whose name contains canary or wiring. |
